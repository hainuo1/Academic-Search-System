# 气象科学研究数据平台 — 项目通读文档 v6.1

> **版本**：v1.0（对应项目 v6.1.0）  
> **更新日期**：2026-07-28  
> **作者**：丁俊杰（南京农业大学，hainuo@stu.njau.edu.cn）  
> **用途**：零基础读者也能从本文档完整了解项目的设计思想、技术架构和功能实现

---

## 一、项目概述

**项目名称**：气象科学研究数据平台（Meteorological Scientific Research Data Platform）

**项目性质**：本科毕业设计 / 个人独立开发项目

**版本演变**：v3.0（原型）→ v4.0 → v5.0（MySQL 版）→ **v6.1（当前版本，PostgreSQL + PostGIS 重构）**

**一句话描述**：一个集学术文献管理与气象灾害数据空间分析于一体的 Web 平台。前端用 Vue 3 构建界面，后端用 FastAPI 提供数据服务，数据库采用 PostgreSQL + PostGIS 空间扩展，并接入了 DeepSeek 大模型进行 AI 科学解读。

**核心定位（双主线）**：

- **学术文献线**：上传 PDF → 自动提取全文 → 多维度搜索（含 PostgreSQL 原生全文检索）→ 收藏 → 引用管理 → 统计排行
- **气象数据线**：台风（IBTrACS）、地震（USGS）、龙卷风（NOAA SPC）三大灾害模块，含列表浏览、详情可视化、PostGIS 邻近空间密度分析、AI 科学解读

**v6.1 相比 v5.0 的关键变化**：

| 方面 | v5.0 | v6.1 |
|------|------|------|
| 后端框架 | Flask | **FastAPI**（自动生成 API 文档，异步就绪） |
| 数据库 | MySQL | **PostgreSQL + PostGIS**（空间分析能力） |
| 全文搜索 | MySQL FULLTEXT（LIKE） | **tsvector + GIN 索引**（专业全文检索） |
| 空间分析 | 不支持 | **PostGIS ST_DWithin**（邻近灾害密度统计） |
| 配置管理 | 环境变量手动读取 | **pydantic-settings + .env**（密钥不入代码库） |

---

## 二、技术栈总览（小白友好版）

如果你不是程序员，可以把下面的技术名词理解成建房子的材料：

| 层级         | 技术                    | 通俗解释                              | 版本            |
| ---------- | --------------------- | --------------------------------- | ------------- |
| **前端框架**   | Vue 3                 | 构建网页界面的"脚手架"，用户看到的所有页面都由它渲染       | 3.5           |
| **前端构建**   | Vite                  | "打包器"，把写好的代码压缩优化，让网页加载更快          | 6.0           |
| **页面跳转**   | Vue Router            | 管理页面之间如何跳转（相当于网站内的"导航地图"）         | 4.5           |
| **数据管理**   | Pinia                 | 在多个页面之间共享数据（比如登录状态）               | 2.3           |
| **网络请求**   | Axios                 | 前端和后端"打电话"的工具，前端问后端要数据            | 1.7           |
| **样式**     | Tailwind CSS          | "装修材料"，让网页好看（通过 CDN 加载，不需要安装）     | CDN           |
| **图标**     | Font Awesome          | 免费图标库，就像 emoji 但更专业               | 4.7           |
| **地图**     | Leaflet               | 在网页上展示交互式地图（台风路径、震中位置）            | 1.9           |
| **图表**     | Canvas 2D             | 浏览器自带的"画板"，用于画柱状图和趋势图             | 内置            |
| **后端框架**   | FastAPI               | 后端"大管家"，接收前端请求、查数据库、返回数据          | 0.115         |
| **服务器**    | Uvicorn               | 让 FastAPI 跑起来的"发动机"               | 0.30          |
| **数据库**    | PostgreSQL            | 存所有数据的"仓库"                        | 15+           |
| **空间扩展**   | PostGIS               | PostgreSQL 的"空间插件"，让数据库能算距离、找邻近点  | 3.x           |
| **数据库驱动**  | SQLAlchemy + psycopg2 | Python 和 PostgreSQL 之间的"翻译官"      | 2.0           |
| **认证**     | PyJWT                 | 生成和验证"通行证"（JWT token），判断你是谁、有没有登录 | 2.10          |
| **密码加密**   | Werkzeug              | 把密码变成不可逆的"乱码"存起来，黑客偷了数据库也看不到明文    | 3.0+          |
| **PDF 解析** | pdfplumber            | 从 PDF 里把文字提取出来，存到数据库供搜索           | 0.11          |
| **验证码**    | Pillow                | 生成注册和找回密码时看到的图片验证码                | 10.4          |
| **AI 接口**  | DeepSeek API          | 调用大语言模型，对气象灾害进行智能分析               | deepseek-chat |
| **一键启动**   | AutoHotkey            | Windows 脚本工具，双击就能同时启动前后端          | v2            |

---

## 三、项目目录结构（逐项说明）

```
6.1版本/
│
├── README.md                              # 项目说明（GitHub 首页展示的就是它）
├── LICENSE                                # 许可证（MIT）
├── .gitignore                             # 告诉 Git 哪些文件不要上传
├── .gitattributes                         # Git LFS 配置（CSV 大文件管理）
├── 启动检索系统.ahk                        # 一键启动脚本（双击即可）
├── 数据库导入文件.py                        # 把三大 CSV 数据集批量写入数据库
│
├── 数据库文件/                             # 数据库相关
│   ├── 建表及模拟数据.sql                   #   创建 17 张表的 SQL（含索引、触发器）
│   |── 数据库设计.md                       #   数据库设计文档（E-R 图 + 表结构详解）
│
├── 数据集/                                # 原始 CSV 数据（通过 Git LFS 管理）
│   ├── 热带气旋/                           #   IBTrACS 台风数据
│   ├── 地震/                              #   USGS 地震数据
│   └── 龙卷风/                             #   NOAA 龙卷风数据
│
├── backend/                               # ── FastAPI 后端 ──
│   ├── .env                               #   环境变量配置（密码、密钥，不提交 Git）
│   ├── .env.example                       #   环境变量模板（安全的，可以提交）
│   ├── requirements.txt                   #   Python 依赖列表
│   ├── app/
│   │   ├── main.py                        #     应用入口（CORS + 路由注册 + 错误处理）
│   │   ├── core/                          #     基础设施层
│   │   │   ├── config.py                  #        配置管理（从 .env 读密码/密钥）
│   │   │   ├── database.py                #        数据库引擎 + 会话工厂
│   │   │   └── security.py                #        JWT token 创建/验证/认证依赖
│   │   ├── models/__init__.py             #        ORM 模型定义（17 张表的结构映射）
│   │   ├── schemas/__init__.py            #        Pydantic 请求/响应校验模型
│   │   ├── services/__init__.py           #        验证码生成 + DeepSeek AI 调用
│   │   └── routers/                       #        API 路由层（10 个模块）
│   │       ├── auth.py                    #          登录/注册/找回密码/验证码
│   │       ├── search.py                  #          文献搜索（tsvector 全文检索）
│   │       ├── document.py                #          文献 CRUD + PDF 上传/下载
│   │       ├── favorites.py               #          收藏/取消收藏
│   │       ├── citation.py                #          引文关系管理
│   │       ├── profile.py                 #          修改邮箱/密码/安全问题
│   │       ├── stats.py                   #          6 维度 TOP10 统计排行
│   │       ├── typhoon.py                 #          台风列表/详情/路径/空间分析/AI
│   │       ├── earthquake.py              #          地震列表/详情/统计/空间分析/AI
│   │       └── tornado.py                 #          龙卷风列表/详情/空间分析/AI
│   └── uploads/                           #       PDF 存储目录
│
└── frontend/                              # ── Vue 3 前端 ──
    ├── index.html                         #   浏览器入口 HTML
    ├── package.json                       #   前端依赖（vue/axios/leaflet 等）
    ├── vite.config.js                     #   Vite 构建配置
    └── src/
        ├── main.js                        #   Vue 应用启动入口
        ├── App.vue                        #   根组件（导航栏 + 页脚 + Flash 消息）
        ├── api/index.js                   #   Axios 实例 + JWT 自动附加 + 401 跳转
        ├── router/index.js                #   路由表（24 条）+ 登录守卫
        ├── stores/                        #   Pinia 全局状态
        │   ├── auth.js                    #     认证状态（token/用户名/ID）
        │   └── flash.js                   #     消息提示（4 秒自动消失）
        └── views/                         #   页面组件（24 个 .vue 文件）
            ├── WelcomeView.vue                # 欢迎首页
            ├── DashboardView.vue              # 功能入口面板
            ├── LoginView.vue                  # 登录页
            ├── RegisterView.vue               # 注册页
            ├── ForgotPasswordView.vue         # 找回密码（两步验证）
            ├── TyphoonView.vue                # 台风列表
            ├── TyphoonDetail.vue              # 台风详情（路径地图 + 强度图表 + 空间密度 + AI）
            ├── EarthquakeView.vue             # 地震列表
            ├── EarthquakeDetail.vue           # 地震详情（震中地图 + 趋势图 + 柱状图 + 空间密度 + AI）
            ├── TornadoView.vue                # 龙卷风列表
            ├── TornadoDetail.vue              # 龙卷风详情（位置地图 + EF 频次图 + 空间密度 + AI）
            ├── SearchView.vue                 # 文献搜索页
            ├── DocumentView.vue               # 文献详情页
            ├── UploadView.vue                 # 文献上传页
            ├── EditDocumentView.vue           # 文献编辑页
            ├── MyDocumentsView.vue            # 我的文献列表
            ├── FavoritesView.vue              # 我的收藏
            ├── HistoryView.vue                # 搜索/浏览历史
            ├── StatsView.vue                  # 统计排行（6 个维度）
            ├── CitationView.vue               # 引文关系管理
            ├── ProfileView.vue                # 个人中心
            └── ErrorView.vue                  # 404 错误页
```

---

## 四、从零开始理解整个系统（非技术人员阅读路线）

如果你是第一次接触这个项目，建议按以下顺序理解：

### 第一步：理解"前后端分离"是什么

想象一个餐厅：

- **前端（Vue 3）** = 服务员。负责给客人看菜单（网页）、记录客人的点单（用户操作）、把菜端上桌（展示数据）
- **后端（FastAPI）** = 厨师。收到服务员传来的订单（API 请求），去冰箱拿食材（查数据库），做好菜（处理数据），递给服务员
- **数据库（PostgreSQL）** = 冰箱。所有原材料（数据）都在里面

前后端之间通过 HTTP 协议（就是网址 `http://localhost:5000/api/xxx`）来传递数据，格式是 JSON（一种像"标签贴在盒子上"一样的数据格式）。

### 第二步：理解用户流程

一个用户从打开网站到使用功能，经历了什么：

1. **打开网页** → 浏览器加载前端代码 → 看到欢迎页
2. **注册账号** → 填表单 → 前端 Axios 向后端发 POST 请求 → 后端校验 → 写入数据库 → 返回成功
3. **登录** → 填用户名密码 → 后端验证 → 返回一个 JWT token（加密的"通行证"）→ 前端存到 localStorage
4. **浏览台风数据** → 前端用 token 请求后端 → 后端验 token → 查 PostgreSQL → 返回数据 → 前端渲染成卡片和图表
5. **上传文献 PDF** → 前端提交文件 → 后端保存文件 → pdfplumber 提取文字 → 触发 tsvector 全文索引 → 以后可以全文搜索

### 第三步：理解关键技术点的价值

- **JWT**：不用每次操作都输密码，登录一次拿 token，后续请求自动带上
- **PostGIS**：不只能查"台风 A 在哪"，还能问"台风 A 周围 500km 内有多少其他台风，平均风速多少"——这是传统数据库做不到的
- **tsvector 全文检索**：不是简单的关键词匹配，而是语言级别的搜索。搜"深度学习"能找出包含"深度神经网络"和"机器学习"的文献，而且比普通 LIKE 搜索快几十倍
- **AI 分析**：不是把原始数据直接发给 AI，而是先把台风的关键参数（最大风速、最低气压、发生时间、路径）整理成一段简洁的文本，再配上角色指令发给 DeepSeek，得到 100-150 字的专业解读

---

## 五、后端架构详解

### 5.1 应用入口：`backend/app/main.py`

FastAPI 应用创建后做了以下事：

- 加载 pydantic-settings 配置（自动读 `.env`）
- 开启 CORS 跨域（允许前端 `localhost:5173` 访问，允许携带 Cookie）
- 注册 10 个路由模块（auth / search / document / favorites / citation / profile / stats / typhoon / earthquake / tornado）
- 全局 404 / 500 错误处理器（返回 JSON 格式，不是默认的 HTML 错误页）
- API 文档自动生成于 `http://localhost:5000/docs`（FastAPI 的一大卖点——不需要手写文档，代码即文档）

### 5.2 配置管理：`backend/app/core/config.py`

使用 **pydantic-settings** 的 `BaseSettings` 类。所有配置项支持从 `.env` 文件自动读取。密钥（数据库密码、JWT 签名密钥、DeepSeek API Key）在代码里是占位值，真实值在 `.env` 里。

**关键配置项**：

| 配置项                               | 说明              | 默认值                                          |
| --------------------------------- | --------------- | -------------------------------------------- |
| `SECRET_KEY`                      | JWT 签名密钥        | 占位值，生产环境必须改                                  |
| `DB_HOST/PORT/USER/PASSWORD/NAME` | PostgreSQL 连接信息 | localhost/5432/postgres/.../AcademicSearchDB |
| `DEEPSEEK_API_KEY`                | DeepSeek API 密钥 | 空，不配则 AI 分析不可用                               |
| `CORS_ORIGINS`                    | 允许的前端地址         | localhost:5173, localhost:3000               |
| `MAX_UPLOAD_SIZE`                 | PDF 上传限制        | 50 MB                                        |

### 5.3 数据库层：`backend/app/core/database.py`

- 用 SQLAlchemy 2.0 创建数据库引擎（同步模式，对应 FastAPI 的同步依赖注入）
- `SessionLocal` = 会话工厂，每次请求创建一个数据库会话
- `get_db()` = FastAPI 依赖注入函数。请求进来时创建会话，请求结束时自动关闭（`yield` + `finally` 模式）
- 项目采用了**混合模式**：ORM 模型在 `models/__init__.py` 里定义了表结构，但实际路由中全部使用 `text()` 写原始 SQL。这样做的好处是精确控制查询性能，代价是不能享受 ORM 的自动关联加载

### 5.4 安全认证：`backend/app/core/security.py`

- **创建 token**：把用户 ID 和用户名打包进 JWT payload，用 HS256 算法 + SECRET_KEY 签名，24 小时过期
- **验证 token**：用同样的密钥解密，如果过期或被篡改则返回 None
- **认证依赖 `get_current_user`**：FastAPI 的 `Depends` 机制——任何路由函数写上 `current_user: dict = Depends(get_current_user)` 就能自动从请求头提取 JWT 并验证。未登录返回 401，已登录返回用户信息

**时间处理改进**：v5.0 用了 Python 已废弃的 `datetime.utcnow()`，v6.1 统一改为 `datetime.now(timezone.utc)`，更规范。

### 5.5 各路由模块详解

#### auth.py — 认证模块

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/login` | POST | 登录。密码 5 次错误锁定 30 分钟；成功则重置计数器 |
| `/api/register` | POST | 注册。前端已有密码长度/一致性校验，后端二次验证 |
| `/api/forgot_password` | POST | 两步找回密码：Step1 验证码 → Step2 安全问答 → 重置密码。安全答案 3 次错误锁定 30 分钟。**安全问题不会在错误响应中泄露**（v6.1 修复） |
| `/api/captcha` | GET | 返回验证码图片，同时用 HttpOnly Cookie 存储正确文本（5 分钟有效）。**Cookie 直接设置在 StreamingResponse 上**（v6.1 修复了 set_cookie 不生效的 bug） |
| `/api/user/me` | GET | 获取当前用户信息（用户名 + 脱敏邮箱） |

**安全设计亮点**：

- 密码使用 Werkzeug 的 `generate_password_hash()` 哈希存储（不可逆）
- 安全答案同样哈希存储
- 登录失败 5 次锁定 30 分钟；找回密码失败 3 次锁定 30 分钟
- 验证码为服务端生成 + HttpOnly Cookie，JavaScript 无法读取

#### search.py — 文献检索模块

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/search` | GET | 多维度文献搜索（标题/作者/分类/关键词/全文），支持高级筛选组合 |
| `/api/history` | GET | 搜索历史 + 浏览历史（分页，窗口化页码） |

**v6.1 核心升级：全文检索从 LIKE → tsvector**

- **旧方式**（v5.0）：`WHERE full_text ILIKE '%关键字%'` —— 全表扫描，速度慢，英文搜索差
- **新方式**（v6.1）：`WHERE full_text_tsv @@ plainto_tsquery('simple', '关键字') OR full_text ILIKE '%关键字%'` —— tsvector 命中走 GIN 索引极快，ILIKE 兜底保证中文短词不丢失
- **新增排序**：全文检索时按 `ts_rank_cd` 相关性降序排列，最相关的结果排前面
- **自动记录**：每次搜索自动写入搜索历史、用户搜索计数、关键词搜索计数三张统计表

搜索方式说明：

| 搜索类型 | 前端选项 | 后端实现 |
|---------|---------|---------|
| 标题 | 📄 标题 | `title ILIKE '%kw%'` |
| 作者 | ✍️ 作者 | `author ILIKE '%kw%'` |
| 分类 | 📂 分类 | `category ILIKE '%kw%'` |
| 关键词 | 🏷️ 关键词 | JOIN document_keyword + keywords 三表 |
| 全文 | 📖 全文 | `tsvector @@ query + ILIKE` 混合策略 |

#### document.py — 文献模块

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/document/{id}` | GET | 文献详情（自动+1 浏览量，记录浏览历史） |
| `/api/upload` | POST | 上传 PDF（pdfplumber 自动提取全文 → 触发 tsvector 触发器） |
| `/api/download/{id}` | GET | 下载 PDF（自动+1 下载量，需登录） |
| `/api/my_documents` | GET | 我的文献列表 |
| `/api/document/{id}/edit-data` | GET | 获取编辑数据（仅上传者本人）→ **v6.1 新增独立端点，解决与详情接口的路由冲突** |
| `/api/document/{id}` | PUT | 保存编辑（v6.1 从 api_route 合并模式拆分为独立端点） |
| `/api/document/{id}` | DELETE | 删除文献（级联清理引用/浏览/收藏/关键词关联 + 物理删除 PDF） |

#### typhoon.py / earthquake.py / tornado.py — 气象模块

三个模块结构高度一致，以台风为例：

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/typhoon/list` | GET | 列表（筛选 + 分页，按最大风速降序） |
| `/api/typhoon/{id}` | GET | 详情（含 **PostGIS 空间密度分析**：500km 内邻近台风统计） |
| `/api/typhoon/{id}/track` | GET | 路径点（用于 Leaflet 地图绘制） |
| `/api/typhoon/ai_analysis` | POST | AI 分析（趋势/对比/影响/出行建议，结果缓存数据库） |

**v6.1 空间密度分析（PostGIS 集成）**：

这是 v6.1 最重要的新功能。以地震为例，当你查看一次 M7.0 大地震时，系统会自动计算：

```sql
-- 300km 范围内的邻近地震统计（使用 geography 真实公里距离）
SELECT
    COUNT(*) AS nearby_count,
    ROUND(AVG(magnitude), 1) AS avg_mag,
    MAX(magnitude) AS max_mag,
    ROUND(AVG(depth), 1) AS avg_depth,
    SUM(CASE WHEN magnitude >= 6.0 THEN 1 ELSE 0 END) AS strong_count
FROM earthquake_info
WHERE ST_DWithin(epicenter::geography, ref_point::geography, 300000);
```

三个模块的空间分析半径不同：台风 500km、地震 300km、龙卷风 100km（根据各自学科的研究尺度设定）。**全部使用 `geography` 类型转换**（非 `geometry`），确保距离按真实公里计算而非按经纬度"度"算。

---

## 六、数据库设计（17 张表总览）

完整的 17 张表 + 索引 + 触发器 + 约束，详见 `数据库文件/数据库设计.md`。

这里只列出分类总览：

| 类别 | 表名 | 一句话描述 |
|------|------|-----------|
| 用户 | `users` | 用户账号、密码哈希、安全问题、锁定状态 |
| 文献 | `documents` | 文献标题/作者/摘要/全文/tsvector 检索向量 |
| 文献 | `keywords` | 关键词字典（去重） |
| 文献 | `document_keyword` | 文献-关键词多对多关联（含 TF-IDF 权重） |
| 文献 | `citation` | 文献引用关系 |
| 文献 | `favorites` | 收藏夹 |
| 文献 | `search_history` | 搜索历史 |
| 文献 | `browse_history` | 浏览历史 |
| 文献 | `user_search_count` | 用户搜索次数统计 |
| 文献 | `keyword_search_count` | 关键词搜索次数统计 |
| 台风 | `typhoon_info` | 台风基本参数 + PostGIS 轨迹线 |
| 台风 | `typhoon_track` | 台风路径观测点 + PostGIS 点 |
| 台风 | `typhoon_ai_analysis` | AI 分析结果缓存 |
| 地震 | `earthquake_info` | 地震参数 + PostGIS 震中点 |
| 地震 | `earthquake_ai_analysis` | AI 分析结果缓存 |
| 龙卷风 | `tornado_info` | 龙卷风参数 + PostGIS 地点 |
| 龙卷风 | `tornado_ai_analysis` | AI 分析结果缓存 |

**关键自动化机制**：

- `trg_documents_tsv` 触发器：每次插入或更新文献时，自动重建 tsvector 全文检索向量
- `set_geom_from_lonlat()` 触发器：每次插入台风路径点/龙卷风地点时，自动从经纬度生成 PostGIS Point geometry
- PostGIS GIST 空间索引 + GIN 全文检索索引：空间查询和全文搜索极速运行

---

## 七、前端架构（24 个页面组件）

### 路由设计（24 条）

**公开路由**（不需要登录）：`/welcome`, `/login`, `/register`, `/forgot_password`

**受保护路由**（需要登录）：`/dashboard`, `/search`, `/document/:id`, `/upload`, `/my_documents`, `/edit_document/:id`, `/favorites`, `/history`, `/stats`, `/profile`, `/citation/:id`, `/typhoon`, `/typhoon/:id`, `/earthquake`, `/earthquake/:id`, `/tornado`, `/tornado/:id`

**导航守卫逻辑**：每次页面跳转时自动检查——没 token 又想进受保护页面 → 踢回 `/welcome`；已有 token 又想去登录页 → 直接跳到 `/dashboard`

### 五个最复杂的页面（技术含金量最高的部分）

#### 1. TyphoonDetail.vue — 台风详情

- **Leaflet 交互地图**：用高德矢量瓦片做底图，绘制台风完整路径（起点绿色、终点红色、峰值金色），按风速大小给路径线段染色（TD 蓝色 → SuperTY 红色），标注风圈半径
- **Canvas 自绘时序图**：风速折线（红色）+ 气压折线（蓝色），双 Y 轴，缺失数据自动跳过（不画 0），鼠标悬停显示十字线 + 详细数值
- **PostGIS 空间密度面板**：展示邻近台风数、平均/最大风速、平均气压
- **AI 分析面板**：4 种分析类型，结果缓存

#### 2. EarthquakeDetail.vue — 地震详情

- **Leaflet 震中地图**：圆形标记按震级自适应半径和颜色（M7+ 红色、M6+ 橙色、M5+ 黄色），外围虚线圆表示震源深度影响范围
- **区域活动趋势图（Canvas）**：邻近地震的震级散点图 + 水平误差气泡，X 轴均匀分布并对齐到年份
- **事件属性对比柱状图（Canvas）**：当前事件 vs 区域历史均值四维对比（震级/深度/台站数/水平误差），缺失数据用 `-` 而非 `0.0` 以避免误解
- **PostGIS 空间密度面板**：邻近地震数、平均/最大震级、平均深度、M≥6 强震数、M≥7 大震数

#### 3. TornadoDetail.vue — 龙卷风详情

- **EF 等级频次分布图（Canvas）**：同州历史龙卷风的 EF0~EF5 分布柱状图，当前事件等级高亮加蓝色边框 + ★ 标识
- **卡片标题格式**：`OH, FIPS0` 格式——州名缩写 + 县级 FIPS 代码，准确区分地理单位
- **PostGIS 空间密度面板**：邻近龙卷风数、总死亡/受伤人数、平均 EF 等级

#### 4. SearchView.vue — 文献搜索

- 主搜索栏（关键词 + 五维检索类型切换）+ 可折叠高级筛选面板（标题/作者/分类/关键词/全文/日期范围）
- 分类浏览标签（点击直接筛选）
- 搜索结果列表（关键词高亮 + 下载按钮）
- 全文检索类型下后端自动返回相关性排序

#### 5. ForgotPasswordView.vue — 找回密码

- 两步式流程：Step1 输入用户名 + 验证码 → 获取安全问题 → Step2 回答安全问题 + 设置新密码
- 前端密码一致性校验（不等直接拦，不浪费一次 API 请求）
- 验证码图片点击刷新

---

## 八、AI 分析模块设计（三模块共用模式）

```
用户点"开始分析" → 前端 POST → 后端查缓存表
                                    ↓
                    命中缓存？──Yes→ 直接返回（毫秒级）
                        ↓ No
                    查数据库构建 prompt
                        ↓
                    调用 DeepSeek API（temperature=0.3, max_tokens=350）
                        ↓
                    结果写入缓存表 + 返回给前端
```

- **Prompt 工程**：每个分析类型有独立 system prompt 设定角色（"你是气象学家""你是灾害评估专家"），user prompt 包含该事件的真实参数
- **输出控制**：100-150 字中文、temperature=0.3 保证稳定输出
- **缓存策略**：首次调用后结果永久缓存到数据库，同一事件同一分析类型不会重复消耗 API 额度

---

## 九、项目启动

### 环境要求

| 软件 | 版本 | 用途 |
|------|------|------|
| Python | 3.10+ | 后端运行 |
| Node.js | 18+ | 前端构建 |
| PostgreSQL | 15+ | 数据库 |
| PostGIS | 3.x | 空间扩展（需在数据库中激活） |

### 启动步骤（5 步）

```bash
# 1. 激活 PostGIS（在 psql 中执行一次）
CREATE EXTENSION IF NOT EXISTS postgis;
CREATE EXTENSION IF NOT EXISTS pg_trgm;

# 2. 创建表结构
psql -U postgres -d AcademicSearchDB -f "数据库文件/建表及模拟数据.sql"

# 3. 配置密码
copy backend\.env.example backend\.env
# 用记事本打开 backend\.env，填入 DB_PASSWORD 和 SECRET_KEY

# 4. 导入数据
set DB_PASSWORD=你的密码
python 数据库导入文件.py

# 5. 启动
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 5000

# 新开终端
cd frontend
npm install
npm run dev

# 浏览器访问 http://localhost:5173/welcome
```

---

## 十、功能全景清单

### 用户系统
- [x] 注册（前端密码一致性校验 + 后端严格格式验证）
- [x] 登录（5 次错误锁定 30 分钟）
- [x] 找回密码（验证码 + 安全问答两阶段，安全问题在错误时不泄露）
- [x] 图形验证码（Pillow 生成 + HttpOnly Cookie）
- [x] 修改邮箱/密码/安全问题

### 文献管理
- [x] PDF 上传 + pdfplumber 全文提取 + tsvector 自动索引
- [x] 五维搜索（标题/作者/分类/关键词/全文）+ 全文 tsvector 相关性排序
- [x] 文献详情（含浏览/下载计数）
- [x] PDF 下载（统一 blob 模式，无需 token 查询参数）
- [x] 文献编辑（独立 edit-data 端点）
- [x] 文献删除（级联清理 + 物理文件删除）
- [x] 我的文献列表
- [x] 收藏/取消收藏切换
- [x] 引文关系管理

### 使用数据
- [x] 搜索历史（自动记录，保留 100 条）
- [x] 浏览历史
- [x] 6 维度 TOP10 统计排行

### 台风模块
- [x] 列表（按最大风速降序，筛选 + 分页）
- [x] 详情（Leaflet 路径地图 + Canvas 双轴时序图 + 风圈展示）
- [x] **PostGIS 500km 邻近台风空间密度统计**
- [x] AI 分析（趋势/对比/影响/出行建议，4 种类型）
- [x] 年度统计

### 地震模块
- [x] 列表（按震级降序，筛选 + 分页）
- [x] 详情（震中地图 + Canvas 趋势图 + Canvas 柱状图对比）
- [x] **PostGIS 300km 邻近地震空间密度统计（含 M≥6/M≥7 分级）**
- [x] AI 分析（趋势/风险/影响，3 种类型）
- [x] 年度统计

### 龙卷风模块
- [x] 列表（按 EF 等级+死亡数降序，筛选 + 分页）
- [x] 详情（位置地图 + Canvas EF 频次分布图）
- [x] **PostGIS 100km 邻近龙卷风空间密度统计（含伤亡汇总）**
- [x] AI 分析（趋势/风险/气候特征，3 种类型）
- [x] 卡片标题 "OH, FIPS0" 格式

### 基础设施
- [x] JWT 认证（PyJWT 2.10 + UTC 时区）
- [x] 统一 JSON 响应 `{ code, message, data }`
- [x] CORS 跨域
- [x] 配置管理（pydantic-settings + .env）
- [x] API 文档自动生成（FastAPI /docs）
- [x] AI 结果缓存
- [x] AutoHotkey 一键启动
- [x] 全局错误捕获（空 catch 已全部替换为 console.error）
- [x] localhost 硬编码已清除

---

## 十一、数据来源说明

| 模块 | 数据来源 | 约记录数 | 原始格式 |
|------|---------|---------|---------|
| 台风 | [IBTrACS](https://www.ncei.noaa.gov/products/international-best-track-archive) v04r01 | ~800,000 路径点 | CSV |
| 地震 | [USGS Earthquake Catalog](https://earthquake.usgs.gov/fdsnws/event/1/) | ~80,000 条 | CSV（分年代） |
| 龙卷风 | [NOAA SPC](https://www.spc.noaa.gov/wcm/#data) | ~70,000 条 | CSV |

---

## 十二、与 v5.0 的关键差异总结

| 方面 | v5.0 | v6.1 |
|------|------|------|
| 后端 | Flask 3.x + PyMySQL | FastAPI 0.115 + SQLAlchemy |
| 数据库 | MySQL 8.0 | PostgreSQL + PostGIS |
| 全文搜索 | LIKE %keyword% | tsvector + GIN + ILIKE 兜底 |
| 空间分析 | 无 | PostGIS ST_DWithin（三模块各自独立半径） |
| 认证时间 | datetime.utcnow（已废弃） | datetime.now(timezone.utc) |
| JWT 依赖 | python-jose | PyJWT 2.10 |
| 配置 | 环境变量 + 硬编码后备 | .env + pydantic-settings，密钥不入库 |
| 文献编辑 | api_route GET/PUT 合并 | 独立端点 edit-data + PUT |
| PDF 下载 | token 查询参数 / 多种模式 | 统一 blob 响应模式 |
| 错误处理 | 7 处空 catch(e){} | 全部替换为 console.error |
| 导入脚本 | 3 个独立脚本，密码硬编码 | 1 个一键脚本，密码从环境变量读 |

---

> **文档版本**：v1.0 | **对应项目版本**：v6.1.0 | **下次更新要点**：随新功能叠加同步更新

© 2026 南京农业大学 · 信息与计算科学专业 · 丁俊杰
