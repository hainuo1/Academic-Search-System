# 气象科学研究数据平台 v6.1 — 开发日志

> 记录时间：2026年7月29日 — 至今

---

## 1. 热带气旋 AI 分析优化

**问题**：AI 提词中，气压数值 `9999` 被当作真实数据而非"无记录"标记，导致 AI 基于错误数据给出分析建议。

**修复**：
- 后端 `typhoon.py` AI 分析接口中，对气压值做过滤：`9999` 等同于无记录，生成提词摘要时替换为"无气压记录"。
- 前端台风详情页的最小气压卡片中，`9999` 显示为"未记录"而非具体数值。

---

## 2. 三大自然灾害模块 AI 分析增加缓存清除

**需求**：原有 AI 分析只能调取已缓存的记录，没有重新生成的机会。

**实现**：
- 地震模块：后端新增 `DELETE /api/earthquake/ai_cache` 接口，前端"开始分析"按钮左侧增加"清理缓存"按钮。
- 台风模块：后端新增 `DELETE /api/typhoon/ai_cache` 接口，前端同样增加缓存清除按钮。
- 龙卷风模块：同上。
- 三个模块的 AI 分析面板布局统一为：`[活动趋势] [风险评估] [影响分析] [清理缓存] [开始分析]`。

---

## 3. 自然灾害分析标签改为只读

**问题**：三个灾害模块的右侧卡片列表中，点击任意卡片会直接跳转到详情页，用户容易误触。左侧大图标是唯一的正门入口。

**修改**：
- 移除了地震列表、台风列表、龙卷风列表卡片上的点击跳转事件。
- 保留左侧页面标题栏区域的导航入口（如 `/typhoon` → `/typhoon/:id`）。
- 卡片仅作信息展示，用户通过左侧大图标区的"查看详情"类按钮进入详情页。

---

## 4. 文献分类标签系统重构

**需求**：分类不应由用户自由输入，应改为从预置分类标签表中选择，支持多选。

**实现**：
- 数据库 `literature_categories` 表已包含 140 个气象科学分类标签。
- 前端新建 `CategorySelectView.vue`：搜索、浏览、多选分类标签，确认后返回上传/编辑页面。
- `UploadView.vue` 和 `EditDocumentView.vue` 中分类字段从自由输入框改为点击按钮跳转分类选择页，已选标签以标签形式回显。
- 路由 `router/index.js` 和 `router/index.ts` 均添加 `/select_category` 路由。

---

## 5. 文献详情页分类显示优化

**修复**：分类标签从混杂在多信息行中改为单独占据一整行，避免文字过密、视觉不清晰。

**变更**：
- `DocumentView.vue` 中分类区域单独占一整行（`flex items-start`），标签以紫色圆角方块排列展示。

---

## 6 & 7. 分类浏览与高级筛选互斥逻辑

**问题**：
- 应用高级筛选时，分类浏览标签仍显示上次点击的分类，实际列表却不是该分类，造成认知偏差。
- 点击分类浏览时应重置所有高级筛选条件。

**修复**：
- `SearchView.vue`：点击任意分类标签时，自动清空所有高级筛选条件（年份、作者、关键词等），只保留分类参数发请求。
- 执行高级筛选时，自动将分类浏览的活跃标签重置为"全部"。
- 两者互斥：同一时刻只有一种筛选方式生效。

---

## 8. 上传文献下载文件名改为标题+作者

**需求**：下载上传的 PDF 时，文件名应为 `论文标题-作者.pdf`，而非服务器上的 UUID 文件名。

**实现**：
- 后端 `document.py` 下载接口 `GET /api/download/{did}`：
  - SQL 查询增加 `author` 字段。
  - `FileResponse` 的 `filename` 参数改为 `f"{title}-{author}.pdf"`。
  - 文件名中的非法字符（`\/:*?"<>|`）用正则替换为下划线。
- 前端 `main.py` CORS 中间件增加 `expose_headers=["Content-Disposition"]`，允许 JS 读取下载响应头。
- 前端四个下载点（DocumentView / SearchView / FavoritesView / MyDocumentsView）均使用不区分大小写的正则 `/filename\*=UTF-8''(.+)/i` 解析文件名。

---

## 9. 添加文献引用关系无响应修复

**问题**：在设置引用关系页面点击"添加引用"按钮无任何反应。

**根因**：`CitationView.vue` 中 `sel.value.push(d)` 在特定情况下未能触发 Vue 响应式更新。

**修复**：
- 改用 `sel.value = [...sel.value, d]` 强制触发响应式。
- 添加 `isSelected()` 检测，已添加的文献按钮显示"已添加"状态。
- 搜索和保存按钮增加 `disabled` 状态与加载动画。
- 按钮标注 `type="button"` 避免意外表单提交。

---

## 10. 文献引用关系双向展示与详情跳转

**需求**：文献详情页展示"本文引用了哪些文献"和"哪些文献引用了本文"两个方向；引用列表中的文献可点击跳转。

**实现**：
- 后端 `document.py` 详情接口改造：
  - `citations`（被哪些文献引用） + `cites`（引用了哪些文献）两个独立数组。
  - 均包含 `id` 和 `title`。
- 前端 `DocumentView.vue`：
  - 引用关系区分为上下两块（绿色=本文引用 / 琥珀色=引用本文），暂无数据时显示"暂无引用文献"。
  - `router-link` 可点击跳转到被引/引用文献的详情页。
  - 添加 `watch(route.params.id)` 监听路由变化，同一页面切换文献时重新加载数据。

---

## 11. 上传文献选分类后表单数据保持

**问题**：去分类选择页选完标签返回后，上传页面的标题、作者等输入全部丢失。

**修复**：
- `UploadView.vue` 和 `EditDocumentView.vue` 均使用 `sessionStorage` 保存表单草稿：
  - 跳转分类选择页前调用 `saveDraft()`。
  - 返回时 `onMounted` 检测 query 参数，从 `sessionStorage` 恢复表单数据。
  - 上传/保存成功后调用 `clearDraft()` 清除。
- PDF 文件对象（`File` 类型）不可序列化，浏览器策略限制无法跨页面保留，需重新选择。

---

## 12. 区域地震活动趋势图改为柱状图

**需求**：原折线图改为柱状图，扩大数据范围，当前事件红色高亮。

**实现**：
- 后端 `earthquake.py` 详情接口：`nearby` 数据不限制数量，取 ±3° 范围内全部邻近事件。
- 前端 `EarthquakeDetail.vue` `drawTrend()` 重写：
  - 柱状图：当前事件红色（`#ef4444`），邻近事件金色（`#f59e0b`，透明度 0.65）。
  - X 轴仅标注起止年份 + 红色标出当前事件年份。
  - 鼠标悬停时右上角 tooltip 显示具体时间、震级、地点。
  - 左下角文字改为"红色=当前事件  金色=邻近事件"和区域范围坐标。

---

## 13. 事件属性对比雷达图回退与数据修正

**需求**：五维雷达图改为震级、定位残差、震源深度、定位台站数、水平误差。

**修改**：
- 标签统一：5 个指标卡 + 雷达图维度标签一致。
- 数据值处理：`null` 或 `0` 的维度在雷达图上标记为灰色"未记录"，不计入区域均值计算。
- 区域均值 `safeAvg()` 自 `nearby` 中过滤掉 `0` 值再求平均，避免未记录数据拉偏。
- 未记录维度的顶点落在雷达图原点（`r=0`），标签标注"未记录"。
- 标签布局：维度标签推至雷达外侧远端（`rad+38px`），数据值以大号字体嵌在顶点旁。
- 图例移至左上角，避免底部被截断。

---

## 14. 地震模块数据范围与显著性替换

**数据排查结论**：
- CSV 数据实际覆盖东亚及周边：纬度 18°~54°N，经度 73°~135°E。
- CSV 文件中没有 `significance` 字段，建表时虽预留但全部为 `0`。
- `mag_nst`（震级计算台站数）是有效的替代指标。

**修改**：
- `EarthquakeView.vue` 页面描述改为"东亚及周边区域"。
- 列表卡片 + 详情 5 卡 + 雷达图：`significance` → `mag_nst`。
- 数据库导入脚本 `数据库导入文件.py` 同步更新：`significance` 读取改为 `mag_nst`。

---

## 15. 其他修复

- **分类页 404 修复**：`router/index.js` 补充缺失的 `/select_category` 路由（该路由原先仅在 `index.ts` 中存在，但 `index.html` 实际加载的是 `main.js`）。
- **UploadView 页面空白修复**：模板中 `v-if="!file.value"` 改为 `v-if="!file"`，Vue 3 模板中 `ref` 自动解包，写 `.value` 会变成 `null.value` 导致报错。
- **PDF 上传界面优化**：选择文件后隐藏原生 `<input>`，显示文件名 + "PDF 文件已就绪" + "重新选择"按钮。
- **龙卷风详情图表底部文字删除**：移除 `共 N 条记录` 描述，避免数据量与实际不符。
- **引用关系页面显示已有引用**：后端 `/api/citation/{did}` 返回 `existing_docs`，前端当前文献卡片下方展示已引用的文献标签列表，支持直接删除已有引用。

---

> **© 2026 南京农业大学 · 信息与计算科学专业 · 丁俊杰**
