# 气象科学研究数据平台 —— 项目文件说明

## 技术栈

| 层级 | 技术 | 说明 |
|:---|:---|:---|
| 前端框架 | Vue 3（Composition API） | SPA 单页应用，组件化开发 |
| 前端构建 | Vite 6 | 开发服务器 + 热更新 + 打包编译 |
| 前端路由 | Vue Router 4 | 前端路由，页面切换不刷新 |
| 前端状态管理 | Pinia 2 | 全局状态管理（用户认证 / 消息提示） |
| HTTP 客户端 | axios | 前端统一请求库，自动携带 JWT Token |
| CSS 框架 | Tailwind CSS（CDN） | 实用优先的 CSS 框架 |
| 地图组件 | Leaflet.js + 高德瓦片 | 热带气旋路径地图可视化 |
| 图表绘制 | Canvas 2D | 强度时序图表手绘 |
| 图标库 | Font Awesome（CDN） | 图标组件 |
| 后端框架 | Flask 3.x | REST API，Blueprint 模块化路由 |
| 数据库 | MySQL 8.0 | PyMySQL 驱动，utf8mb4 字符集 |
| 认证 | JWT（PyJWT） | 无状态 Token 认证 |
| AI 接口 | DeepSeek API | 热带气旋数据智能科学解读 |
| 数据来源 | IBTrACS（NOAA） | 国际热带气旋最佳路径数据集 |
| PDF 解析 | pdfplumber | 上传时自动提取全文 |
| 密码加密 | Werkzeug Security | PBKDF2 哈希 |
| 跨域 | flask-cors | 允许前端 5173 → 后端 5000 跨端口通信 |
| 一键启动 | AutoHotkey v2 | 双击启动前后端 + 打开浏览器 |

---

## 目录结构

```
4.0版本-台风模块/
│
├── README.md                    # 项目主文档
├── requirements.txt             # Python 依赖清单（根目录，向后兼容）
├── .gitignore                   # Git 忽略规则
├── 项目各文件说明.md              # 本文件
├── 数据库设计.md                 # 数据库设计文档（ER 图 + 表结构 + 约束）
├── 台风模块建表.sql              # 热带气旋相关表建表 SQL
├── import_ibtracs.py            # IBTrACS 数据导入脚本
├── 启动检索系统.ahk              # AutoHotkey 一键启动脚本
│
├── backend/                     # Flask API 后端
│   ├── app.py                   #   应用入口：创建 Flask、注册 Blueprint、配置 CORS
│   ├── config.py                #   集中配置：密钥、数据库连接、上传限制、DeepSeek API
│   ├── db.py                    #   数据库层：请求级连接管理 + Row 类封装
│   ├── utils.py                 #   工具函数：JWT 生成/验证、分页、@login_required 门禁
│   ├── captcha.py               #   图形验证码生成
│   ├── requirements.txt         #   后端 Python 依赖
│   ├── uploads/                 #   PDF 存储目录（运行时自动创建）
│   └── routes/                  #   蓝图路由（8 个模块）
│       ├── __init__.py          #     包初始化文件
│       ├── auth.py              #     用户认证：登录、注册、找回密码、验证码
│       ├── search.py            #     文献检索：多条件检索、检索/浏览历史
│       ├── document.py          #     文献管理：上传、下载、编辑、删除
│       ├── favorites.py         #     文献收藏：收藏/取消（Toggle 模式）
│       ├── citation.py          #     引文分析：引用关系查看、搜索、保存
│       ├── profile.py           #     个人中心：改邮箱、改密码、改安全问题
│       ├── stats.py             #     统计总览：6 类 TOP10 排行
│       └── typhoon.py           #     灾害数据分析：热带气旋列表、路径、统计、AI 分析
│
└── frontend/                    # Vue 3 前端项目
    ├── index.html               #   浏览器入口文件（Vue 挂载点）
    ├── package.json             #   前端依赖清单
    ├── vite.config.js           #   Vite 构建配置
    └── src/
        ├── main.js              #   Vue 启动器：创建应用、挂载路由/状态管理
        ├── App.vue              #   根组件：导航栏 + Flash 消息 + 公共页脚
        ├── api/
        │   └── index.js         #   axios 实例：请求拦截（自动带 Token）、响应拦截（401 跳转）
        ├── router/
        │   └── index.js         #   路由表：18 个路由 + 路由守卫
        ├── stores/
        │   ├── auth.js           #   用户认证状态（token / username / userId）
        │   └── flash.js          #   全局消息提示状态
        └── views/                #   18 个页面组件
            ├── WelcomeView.vue       # 欢迎首页（平台介绍 + 登录入口）
            ├── DashboardView.vue     # 功能入口页（两大模块卡片 + 子功能入口）
            ├── TyphoonView.vue       # 热带气旋列表（搜索、筛选、分页）
            ├── TyphoonDetail.vue     # 热带气旋详情（路径地图 + 时序图 + AI 分析）
            ├── LoginView.vue         # 登录页
            ├── RegisterView.vue      # 注册页
            ├── ForgotPasswordView.vue # 找回密码
            ├── SearchView.vue        # 文献检索（关键词 + 高级筛选）
            ├── DocumentView.vue      # 文献详情
            ├── UploadView.vue        # 文献上传
            ├── EditDocumentView.vue   # 文献编辑
            ├── MyDocumentsView.vue    # 个人文献库
            ├── FavoritesView.vue      # 文献收藏
            ├── HistoryView.vue        # 使用记录（检索 + 浏览历史）
            ├── StatsView.vue          # 统计总览
            ├── ProfileView.vue        # 个人中心
            ├── CitationView.vue       # 引文关系设置
            └── ErrorView.vue          # 404 错误页
```

---

## 核心模块说明

### app.py — 后端入口

`create_app()` 完成：加载 Config → 创建 uploads 目录 → 注册 8 个 Blueprint → 配置 CORS（允许 localhost:5173） → 注册 404/500 错误处理器 → `app.run(port=5000)`。所有路由仅返回 JSON。

### db.py — 数据库层

封装 `Row` 类，让查询结果支持 `row.FieldName` 访问。`get_connection()` 在请求级别复用同一连接，`close_connection()` 随请求结束自动释放。使用 PyMySQL 的 DictCursor，字段按 PascalCase 映射。

### routes/ — 蓝图路由（8 个模块）

| 蓝图 | 核心路由 | 关键逻辑 |
|:---|:---|:---|
| **auth** | /api/login, /api/register, /api/forgot_password, /api/captcha, /api/user/me | 失败计数 + 锁定机制；两步密码找回；答案哈希不可逆存储 |
| **search** | /api/search, /api/history | 5 种检索方式；高级筛选含日期、分类；关键词去重；自动记录检索历史 |
| **document** | /api/upload, /api/download, /api/document | PDF 全文自动提取；关键词字典化存储；级联删除 |
| **favorites** | /api/favorites, /api/toggle_favorite | 收藏/取消切换（Toggle 模式） |
| **citation** | /api/citation, /api/search_citation, /api/save_citation | 仅上传者可设引用；搜索时排除自身 |
| **profile** | /api/profile, /api/profile/email, /api/profile/password, /api/profile/security | 三面板修改均需验证密码；邮箱脱敏显示 |
| **stats** | /api/stats | 6 个 TOP10 排行 |
| **typhoon** | /api/typhoon/list, /api/typhoon/<id>, /api/typhoon/<id>/track, /api/typhoon/stats/yearly, /api/typhoon/ai_analysis | 热带气旋数据查询、路径可视化数据、年度统计、DeepSeek AI 分析（含缓存） |

### utils.py — 工具箱

- `create_token()`：生成 JWT，有效期 24 小时
- `verify_token()`：验证 JWT 签名和过期时间
- `login_required`：装饰器，Bearer Token 验证，未登录返回 401
- `process_keywords()`：关键词字典化存储 + 关联表维护
- `get_pagination()`：通用分页计算器

### import_ibtracs.py — 数据导入

从 NOAA IBTrACS CSV 下载并解析热带气旋最佳路径数据，过滤太平洋区域（100°E-180°E, 0°-60°N），写入 TyphoonInfo / TyphoonTrack 表。支持年份过滤、dry-run 预览模式。

### 前端架构

**入口链路**：`index.html` → `main.js` → 创建 Vue 应用 → 挂载 Router + Pinia → `App.vue` → `<router-view>` 动态切换页面。

**路由守卫**：
- 未登录用户 → `/welcome` 欢迎页
- 已登录用户访问公开页面 → `/dashboard` 功能入口页

**axios 拦截器**：
- 请求：自动附加 JWT Token
- 响应：401 时清空登录状态并跳转 `/welcome`

**状态管理**：
- `auth.js`：token / username / userId 持久化到 localStorage
- `flash.js`：全局消息提示，4 秒自动消失

---

## 数据库（12 张表）

详见 `数据库设计.md`。核心关系：

```
Users ──┬── Documents ──┬── DocumentKeyword ── Keywords
        │               ├── Citation (Source/Target)
        ├── Favorites
        ├── BrowseHistory
        ├── SearchHistory
        ├── UserSearchCount
        └── KeywordSearchCount

TyphoonInfo ── TyphoonTrack
             └── TyphoonAIAnalysis
```

---

## 启动方式

### 方式一：AutoHotkey 一键启动

双击 `启动检索系统.ahk` → 自动启动后端 → 前端 → 打开浏览器。

### 方式二：手动启动

```bash
# 后端
cd backend
pip install -r requirements.txt
python app.py  # http://localhost:5000

# 前端
cd frontend
npm install  # 首次
npm run dev  # http://localhost:5173
```

浏览器访问 `http://localhost:5173/welcome`

---

## 环境要求

| 组件 | 版本要求 |
|:---|:---|
| Python | 3.10+ |
| Node.js | 18+（LTS） |
| npm | 9+ |
| MySQL | 8.0+ |
| AutoHotkey | v2.0（仅一键启动需要） |

---

> 数据库设计详见 `数据库设计.md`。
