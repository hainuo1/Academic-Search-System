# 学术文献检索系统 4.0 — 项目文件说明

## 技术栈

| 层级 | 技术 | 说明 |
|:---|:---|:---|
| 前端框架 | Vue 3（Composition API） | SPA 单页应用，组件化开发 |
| 前端构建 | Vite 6 | 开发服务器 + 热更新 + 打包编译 |
| 前端路由 | Vue Router 4 | 前端路由，页面切换不刷新 |
| 前端状态管理 | Pinia 2 | 全局状态管理（用户认证 / 消息提示） |
| HTTP 客户端 | axios | 前端统一请求库，自动携带 JWT Token |
| CSS 框架 | Tailwind CSS（CDN） | 实用优先的 CSS 框架 |
| 图标库 | Font Awesome（CDN） | 图标组件 |
| 后端框架 | Flask 3.x | REST API，Blueprint 模块化路由 |
| 数据库 | MySQL | PyMySQL 驱动，utf8mb4 字符集 |
| 认证 | JWT（PyJWT） | 无状态 Token 认证 |
| PDF 解析 | pdfplumber | 上传时自动提取全文 |
| 密码加密 | Werkzeug Security | PBKDF2 哈希 |
| 跨域 | flask-cors | 允许前端 5173 → 后端 5000 跨端口通信 |
| 一键启动 | AutoHotkey v2 | 双击启动前后端 + 打开浏览器 |

---

## 目录结构

```
检索系统4.0/
│
├── README.md                    # 项目主文档
├── requirements.txt             # Python 后端依赖清单
├── .gitignore                   # Git 忽略规则
├── 项目各文件说明.md              # 本文件
├── 数据库设计.md                 # 数据库设计文档（ER 图 + 表结构 + 约束）
├── 启动检索系统.ahk              # AutoHotkey 一键启动脚本
│
├── backend/                     # Flask API 后端
│   ├── app.py                   #   应用入口：创建 Flask、注册 Blueprint、配置 CORS
│   ├── config.py                #   集中配置：密钥、数据库连接、上传限制
│   ├── db.py                    #   数据库层：请求级连接管理 + Row 类封装
│   ├── utils.py                 #   工具函数：JWT 生成/验证、分页、@login_required 门禁
│   ├── captcha.py               #   图形验证码生成
│   ├── requirements.txt         #   后端 Python 依赖清单（同上）
│   ├── uploads/                 #   PDF 存储目录（运行时自动创建）
│   └── routes/                  #   蓝图路由（7 个模块）
│       ├── __init__.py          #     包初始化文件
│       ├── auth.py              #     登录、注册、找回密码、验证码、当前用户
│       ├── search.py            #     多条件检索、检索/浏览历史
│       ├── document.py          #     文献 CRUD、上传、下载、浏览记录
│       ├── favorites.py         #     收藏列表、收藏/取消收藏（Toggle 模式）
│       ├── citation.py          #     引用关系查看、搜索、保存
│       ├── profile.py           #     个人信息、改邮箱、改密码、改安全问题
│       └── stats.py             #     6 类 TOP10 统计排行
│
└── frontend/                    # Vue 3 前端项目
    ├── index.html               #   浏览器入口文件（Vue 挂载点）
    ├── package.json             #   前端依赖清单（npm）
    ├── vite.config.js           #   Vite 构建配置
    └── src/
        ├── main.js              #   Vue 启动器：创建应用、挂载路由/状态管理
        ├── App.vue              #   根组件：导航栏 + Flash 消息 + 公共页脚
        ├── api/
        │   └── index.js         #   axios 实例：请求拦截（自动带 Token）、响应拦截（401 跳转）
        ├── router/
        │   └── index.js         #   路由表：15 个路由定义 + 路由守卫（登录拦截）
        ├── stores/
        │   ├── auth.js           #   用户认证状态（token / username / userId）
        │   └── flash.js          #   全局消息提示状态
        └── views/                #   15 个页面组件
            ├── WelcomeView.vue       # 欢迎首页（功能展示 + 登录/注册入口）
            ├── LoginView.vue         # 登录页
            ├── RegisterView.vue      # 注册页
            ├── ForgotPasswordView.vue # 找回密码
            ├── SearchView.vue        # 搜索主页（关键词搜索 + 高级筛选 + 分类浏览）
            ├── DocumentView.vue      # 论文详情页
            ├── UploadView.vue        # 上传文献
            ├── EditDocumentView.vue   # 编辑文献
            ├── MyDocumentsView.vue    # 我的文献列表
            ├── FavoritesView.vue      # 我的收藏
            ├── HistoryView.vue        # 检索历史 + 浏览历史
            ├── StatsView.vue          # 系统数据统计
            ├── ProfileView.vue        # 个人信息管理
            ├── CitationView.vue       # 引用关系设置
            └── ErrorView.vue          # 404 错误页
```

---

## 核心模块说明

### app.py — 后端入口

`create_app()` 完成：加载 Config → 创建 uploads 目录 → 注册 7 个 Blueprint → 配置 CORS（允许 localhost:5173） → 注册 404/500 错误处理器 → `app.run(port=5000)`。

不再渲染任何 HTML 页面。所有路由仅返回 JSON。

### db.py — 数据库层

封装 `Row` 类，让查询结果支持 `row.FieldName` 访问（例如 `paper.Title`）。`get_connection()` 在请求级别复用同一连接，`close_connection()` 随请求结束自动释放。使用 PyMySQL 的 DictCursor，所有字段按 PascalCase 映射。

### routes/ — 蓝图路由（7 个模块）

| 蓝图 | 核心路由 | 关键逻辑 |
|:---|:---|:---|
| **auth** | /api/login, /api/register, /api/forgot_password, /api/captcha, /api/user/me | 失败计数 + 30 分钟锁定；两步找回密码（验证码→安全问题）；答案哈希不可逆存储；密码哈希存储 + 校验 |
| **search** | /api/search, /api/history | 5 种检索方式（标题/作者/分类/关键词/全文）；高级筛选含日期范围、分类过滤；keyword 类型用 DISTINCT 防重复；每次检索自动记录搜索历史 + 更新关键词/用户统计 |
| **document** | /api/upload, /api/download, /api/document | PDF 全文自动提取；关键词字典化存储；删除时级联清理引用、收藏、关键词关联 |
| **favorites** | /api/favorites, /api/toggle_favorite | 同一接口实现收藏/取消收藏切换（Toggle 模式） |
| **citation** | /api/citation, /api/search_citation, /api/save_citation | 仅上传者可设置引用；搜索时排除自身；保存时先删旧关系再插新的 |
| **profile** | /api/profile, /api/profile/email, /api/profile/password, /api/profile/security | 三面板修改均需验证当前密码；邮箱脱敏显示 |
| **stats** | /api/stats | 6 个 TOP10 排行：热门关键词、活跃用户、高被引、高浏览、高下载、高收藏 |

### utils.py — 工具箱

- `create_token(user_id, username)`：生成 JWT，有效期 24 小时
- `verify_token(token)`：验证 JWT 签名和过期时间
- `login_required`：装饰器，从请求头提取 Bearer Token 并验证，未登录返回 401
- `process_keywords()`：关键词字典化存储 + 关联表维护
- `get_pagination()`：通用分页计算器

### 前端架构

**入口链路**：`index.html` → `main.js` → 创建 Vue 应用 → 挂载 Router + Pinia → 渲染 `App.vue` → `<router-view>` 区域动态切换页面组件。

**路由守卫**（`router/index.js`）：
- 未登录用户访问需要登录的页面 → 自动跳转到 `/welcome` 欢迎页
- 已登录用户访问公开页面（登录/注册） → 自动跳转到 `/search` 搜索页

**axios 拦截器**（`api/index.js`）：
- 请求拦截：自动从 localStorage 读取 token 添加到 Authorization 头
- 响应拦截：401 时自动清空登录状态并跳转到 `/welcome`

**状态管理**（Pinia）：
- `auth.js`：token / username / userId，同时同步到 localStorage 持久化
- `flash.js`：全局消息提示，4 秒自动消失

---

## 数据库（10 张表）

详见 `数据库设计.md`。核心关系：

```
Users ──┬── Documents ──┬── DocumentKeyword ── Keywords
        │               ├── Citation (Source/Target)
        ├── Favorites
        ├── BrowseHistory
        ├── SearchHistory
        └── UserSearchCount
```

---

## 启动方式

### 方式一：AutoHotkey 一键启动（推荐）

双击项目根目录的 `启动检索系统.ahk` → 自动启动后端 (5000) → 启动前端 (5173) → 打开浏览器访问欢迎页。

### 方式二：手动启动

**1. 启动后端**
```bash
cd backend
pip install -r requirements.txt
python app.py
# 后端运行在 http://localhost:5000
```

**2. 启动前端**
```bash
cd frontend
npm install          # 首次运行需要
npm run dev
# 前端运行在 http://localhost:5173
```

**3. 打开浏览器访问** `http://localhost:5173/welcome`

### 环境要求

| 组件 | 版本要求 |
|:---|:---|
| Python | 3.10+ |
| Node.js | 18+（LTS） |
| npm | 9+ |
| MySQL | 8.0+ |
| AutoHotkey | v2.0（仅一键启动需要） |

### 数据库初始化

1. 创建 MySQL 数据库：`CREATE DATABASE AcademicSearchDB CHARACTER SET utf8mb4;`
2. 运行 `数据库设计.md` 中的建表 SQL
3. 修改 `backend/config.py` 中的数据库密码（或设置环境变量 `DB_PASSWORD`）

---

> 详细设计文档见 `数据库设计.md`，版本演进历史见手册第一篇序章。
