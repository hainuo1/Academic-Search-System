# 学术文献检索系统 — 项目文档

## 技术栈

| 层级     | 技术                    | 说明                     |
| :----- | :-------------------- | :--------------------- |
| 后端框架   | Flask 3.x             | Blueprint 模块化路由        |
| 数据库    | MySQL                 | PyMySQL 驱动，utf8mb4 字符集 |
| 前端     | Tailwind CSS + Jinja2 | CDN 引入，零构建步骤           |
| PDF 解析 | pdfplumber            | 上传时自动提取全文              |
| 密码加密   | Werkzeug Security     | PBKDF2 哈希              |

---

## 目录结构

```
检索系统2.0/
├── app.py              # 应用工厂入口
├── config.py           # 集中配置（密钥、数据库、上传限制）
├── db.py               # 请求级连接管理 + Row 类封装
├── captcha.py          # 图形验证码生成
├── utils.py            # 分页函数、关键词处理
├── requirements.txt    # 依赖清单
├── routes/             # 蓝图路由
│   ├── auth.py         #   登录、注册、找回密码、验证码
│   ├── document.py     #   文献 CRUD、上传、下载
│   ├── search.py       #   多条件检索、检索/浏览历史
│   ├── favorites.py    #   收藏（AJAX 开关）
│   ├── citation.py     #   引用关系设置
│   ├── profile.py      #   个人信息修改
│   └── stats.py        #   系统统计排行
├── templates/          # Jinja2 模板（继承 base.html）
├── static/
│   ├── css/main.css    #   全局样式 + 动画
│   └── js/             #   7 个交互脚本
├── uploads/            # PDF 存储目录（运行时创建）
├── 数据库设计.md        # 数据库设计文档
└── Bug审查报告.md       # 审查与修复记录
```

---

## 核心模块

### app.py — 应用工厂

`create_app()` 完成：加载配置 → 创建 uploads 目录 → 注册 7 个蓝图 → 注册 `teardown_appcontext` 自动关库 → 注册 `highlight` 过滤器（搜索结果关键词高亮） → 注册 404/500 错误处理器。

### db.py — 数据库层

封装了 `Row` 类，让查询结果同时支持三种访问方式：`row.FieldName`、`row['FieldName']`、`row[0]`。`get_connection()` 在请求级别复用同一连接，`close_connection()` 随请求结束自动释放。`_ConnectionWrapper` 包装了 commit/rollback/cursor，业务代码无需关心底层驱动细节。

### routes/ — 蓝图路由

| 蓝图 | 核心路由 | 关键逻辑 |
|:---|:---|:---|
| **auth** | /login, /register, /forgot_password, /captcha | 失败计数 + 30 分钟锁定；两步找回密码（验证码→安全问题）；答案哈希不可逆存储 |
| **document** | /upload, /download, /edit, /delete | PDF 全文提取；关键词字典化存储；删除时级联清理引用、收藏、关键词关联及孤立词 |
| **search** | /search, /history | 标题/作者/分类/关键词/全文 5 种检索；高级筛选含关键词 JOIN 查询；keyword 类型用 DISTINCT 防重复；检索/浏览历史各自独立分页 |
| **favorites** | /toggle_favorite | AJAX 开关，先校验文献存在性再操作 |
| **citation** | /citation, /save_citation | 仅上传者可设置；搜索排除自身；保存时校验目标文献存在性 + 防止自引用 |
| **profile** | /profile | 三面板：改邮箱/改密码/改安全问题，均需验证当前密码 |
| **stats** | /stats | 6 个 TOP 10 排行：热门关键词、活跃用户、高被引、浏览/下载/收藏排行 |

### 前端

所有模板继承 `base.html`（导航栏 + Flash 消息 + 页脚），页面专属 JS 通过 `{% block extra_script %}` 注入。交互脚本职责单一：`app.js` 折叠面板，`index.js` 打字机效果，`captcha.js` 验证码刷新，`document.js` 收藏切换，`favorites.js` 取消收藏，`citation.js` 引用搜索，`upload.js` 防重复提交。

---

## 数据库（10 张表）

详见 `数据库设计.md`。核心关系：

```
Users ──┬── Documents ──┬── DocumentKeyword ── Keywords
        │               ├── Citation (source/target)
        ├── Favorites
        ├── BrowseHistory
        ├── SearchHistory
        └── UserSearchCount
```

计数统计冗余在 `Documents` 表（ViewCount/DownloadCount），检索统计通过 `UserSearchCount` 和 `KeywordSearchCount` 的 `ON DUPLICATE KEY UPDATE` 原子递增。

---

## 启动方式

```bash
cd 检索系统2.0
pip install -r requirements.txt
export DB_PASSWORD="你的密码"      # 或者用默认值
python app.py                       # 默认 http://127.0.0.1:5000
```
