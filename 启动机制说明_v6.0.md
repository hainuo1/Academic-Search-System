# 气象科学研究数据平台 v6.0 — 启动机制说明

> **版本**：v1.0（对应项目 v6.0.0）  
> **更新日期**：2026-07-28  
> **用途**：面向初学者，解释"启动"到底是什么、为什么前后端要分别启动、以及 AutoHotkey 一键启动脚本如何工作

---

## 一、什么叫"启动"？

**启动**就是让一个程序从硬盘上的静态文件，变成内存中正在运行的进程，开始监听请求或处理任务。

### 启动前 vs 启动后

```
启动前：
  main.py ──→ 只是存储在硬盘里的一个文件
  浏览器访问 → 无法连接（没有服务在运行）

启动后：
  main.py ──→ Python 解释器加载 ──→ FastAPI 对象创建 
            ──→ Uvicorn 监听 5000 端口 ──→ 等待 HTTP 请求
  浏览器访问 → 正常返回 JSON 数据
```

### 通俗类比

你写了一个函数 `def get_weather(): return {"temperature": 25}`。这段代码只是**定义了功能**——计算机不知道什么时候调用它、谁可以调用它、从哪个网络地址调用它。

执行 `uvicorn main:app --port 5000` 之后，FastAPI 才变成一台"开着门营业的服务窗口"——任何从 5000 端口发来的 HTTP 请求都会由它接收并处理。

---

## 二、为什么前端也需要启动？

以前（传统网页时代）：

```
双击 index.html → 浏览器直接打开 → 立即可看
```

现在（Vue 项目）：

```
src/App.vue ──→ 浏览器不认识 .vue 文件
             ──→ 需要 Vite 编译转换
             ──→ 转换为浏览器能懂的 HTML + CSS + JavaScript
```

Vite 的编译流程：

```
.vue 单文件组件（template + script + style）
    ↓ Vite 开发服务器
拆分为独立的 HTML / CSS / JavaScript
    ↓ 浏览器加载
用户看到完整页面
```

**所以"启动前端"启动的不是网页本身，而是一个编译转换工具（Vite 开发服务器），它负责把 Vue 源码实时转成浏览器可执行的文件。**

---

## 三、为什么前后端需要分别启动？

因为本项目采用**前后端分离架构**——前端和后端是两个完全独立的程序，各自有自己的运行环境和启动命令。

```
浏览器 (用户的电脑)

  ↓ 页面渲染

Vue 前端 (Vite 开发服务器, 端口 5173)
  负责：页面结构、样式、交互、地图、图表

  ↓ Axios 发送 HTTP 请求

FastAPI 后端 (Uvicorn 服务器, 端口 5000)
  负责：认证、业务逻辑、数据库查询、AI 调用

  ↓ SQLAlchemy + psycopg2

PostgreSQL + PostGIS 数据库 (端口 5432)
  负责：持久化存储所有数据
```

前端和后端不是同一个程序，所以必须分别启动。它们通过 HTTP 协议通信——前端在 5173 端口运行，后端在 5000 端口运行，互不干扰。

---

## 四、完整启动涉及哪几层？

实际上有**三层**需要处于运行状态：

| 层级 | 启动命令 | 端口 | 作用 |
|------|---------|------|------|
| 数据库 | PostgreSQL 服务（通常开机自启） | 5432 | 数据存储与查询 |
| 后端 | `uvicorn app.main:app --port 5000` | 5000 | API 服务、业务逻辑 |
| 前端 | `npm run dev`（Vite 开发服务器） | 5173 | 网页编译与呈现 |

**数据库通常只需启动一次**——PostgreSQL 安装后配置为开机自启的 Windows 服务，每次开机就已经在运行。因此日常开发实际上只需要启动后端和前端两层。

---

## 五、AutoHotkey 一键启动脚本详解

项目根目录下的 `启动检索系统.ahk` 是一个 Windows 自动化脚本，双击即可完成全部启动操作。下面逐行解释。

### 脚本头部配置

```autohotkey
#Requires AutoHotkey v2.0          ; 要求 AutoHotkey v2.0 版本
#SingleInstance Force              ; 防止重复运行（如果已有实例则强制替换）
SetWorkingDir A_ScriptDir          ; 将工作目录设为脚本所在目录
```

### 路径配置区

```autohotkey
backDir  := "C:\Users\hainuo\Desktop\检索系统\6.0版本\backend"
frontDir := "C:\Users\hainuo\Desktop\检索系统\6.0版本\frontend"
pyCmd := "python -m uvicorn"       ; 后端启动命令（如果使用 conda 可以改为完整路径）
browserUrl := "http://localhost:5173/login"
```

**需要修改的部分**：如果把项目移动到别的文件夹，只需改 `backDir` 和 `frontDir` 两个路径即可，其余不用动。

### 第一步：启动后端

```autohotkey
Run(A_ComSpec ' /k "title 检索系统-后端 && echo [后端] 正在启动 FastAPI 服务... && '
    pyCmd ' app.main:app --reload --host 0.0.0.0 --port 5000"',
    backDir, 'Min')
Sleep(3000)
```

逐段解释：
- `A_ComSpec` — Windows 命令行程序（cmd.exe）
- `/k` — 执行完命令后保持窗口打开（方便查看日志和手动 Ctrl+C 关闭）
- `title 检索系统-后端` — 把命令行窗口标题设为"检索系统-后端"，方便区分
- `echo [后端] 正在启动 FastAPI 服务...` — 在窗口中打印提示文字
- `python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 5000` — 核心启动命令：
  - `-m uvicorn` — 以模块方式运行 Uvicorn 服务器
  - `app.main:app` — 模块路径为 `app/main.py`，FastAPI 实例名为 `app`
  - `--reload` — 代码修改后自动重启（开发模式）
  - `--host 0.0.0.0` — 监听所有网络接口（允许从其他设备访问）
  - `--port 5000` — 在 5000 端口提供服务
- `backDir` — 在 backend 目录下执行命令
- `Min` — 窗口启动后自动最小化（不遮挡屏幕）
- `Sleep(3000)` — 等待 3 秒，给后端足够的时间完成启动

### 第二步：启动前端

```autohotkey
Run(A_ComSpec ' /k "title 检索系统-前端 && echo [前端] 正在启动 Vite 开发服务器... && npm run dev"',
    frontDir, 'Min')
Sleep(2500)
```

与后端类似：
- 在 `frontDir` 目录下执行 `npm run dev`
- Vite 启动后监听 5173 端口
- 等待 2.5 秒确保前端启动完成

### 第三步：打开浏览器

```autohotkey
Run(browserUrl)    ; 用系统默认浏览器打开 http://localhost:5173/login
ExitApp()          ; 脚本自身退出（前后端进程在各自命令行窗口中继续运行）
```

**关键点**：`ExitApp()` 退出的只是 AutoHotkey 脚本本身，不代表关闭前后端——后端和前端分别在两个独立的命令行窗口中运行，互不依赖。

---

## 六、如何关闭

关闭方式很简单：
- 前端窗口：直接 Ctrl+C 或点击 X 关闭命令行窗口
- 后端窗口：同上
- 数据库：通常不需要关闭（系统服务自动管理）

三个窗口各自独立，关掉前端不影响后端继续运行，关掉后端不影响数据库继续运行。

---

## 七、启动流程图

```
双击 启动检索系统.ahk
        │
        ▼
   ┌─ 步骤 1：启动后端 ──────────────────────────┐
   │  cd backend                                  │
   │  python -m uvicorn app.main:app --port 5000  │
   │  窗口标题：检索系统-后端                        │
   │  状态：最小化，等待请求                         │
   └──────────────────────────────────────────────┘
        │ 等待 3 秒
        ▼
   ┌─ 步骤 2：启动前端 ──────────────────────────┐
   │  cd frontend                                 │
   │  npm run dev                                 │
   │  窗口标题：检索系统-前端                        │
   │  状态：最小化，编译 .vue 文件                   │
   └──────────────────────────────────────────────┘
        │ 等待 2.5 秒
        ▼
   ┌─ 步骤 3：打开浏览器 ────────────────────────┐
   │  访问 http://localhost:5173/login            │
   │  AutoHotkey 脚本退出                          │
   └──────────────────────────────────────────────┘

启动完成：共 3 个窗口（后端 + 前端 + 浏览器）
```

---

> **文档版本**：v1.0

© 2026 南京农业大学 · 信息与计算科学专业 · 丁俊杰
