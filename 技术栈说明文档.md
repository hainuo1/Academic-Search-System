# 气象科学研究数据平台 — 技术栈说明

> **用途**：面向零基础读者，逐层讲解本项目的全部技术选型——每一项技术是什么、为什么选它、在本项目中起什么作用。
> **项目**：MeteoScholar v6.1（前后端分离科研平台）
> **作者**：丁俊杰（南京农业大学，hainuo@stu.njau.edu.cn）

---

## 阅读指南

如果你是零基础，建议按顺序从头读到尾。每一层都以上一层为基础，层层递进——先理解网页怎么出来（第一层），再理解页面怎么跳转和共享数据（第二层），接着理解前端怎么和后端通信、怎么登录认证（第三层）……这样读完一遍，你对整个系统的理解就是连贯的而不是零散的。

如果多年后你需要快速找回记忆：先看下面的"整体架构"图回忆数据流向，再跳到对应的大层标题，每项技术的最后一句引文（`>` 开头的那行）会告诉你它在项目中的定位。

---

## 整体架构

用户打开浏览器访问系统时，数据是这样流转的：

```
浏览器 (localhost:5173)
    ↓
Vite 开发服务器 → Vue 3 SPA（前端界面）
    ↓ Axios + JWT Token
Uvicorn ASGI 服务器 → FastAPI（后端 API）
    ↓ SQLAlchemy + psycopg2
PostgreSQL + PostGIS（数据库，localhost:5432）
    ↓ HTTP 调用
DeepSeek API（AI 分析）
```

整个系统分为**七个层面**，共 22 项核心技术：

| 层面 | 技术 | 一句话定位 |
|---|---|---|
| 一、前端框架与工程化 | Vue、Vite | 网页怎么出来的 |
| 二、状态与页面导航 | Pinia、Vue Router | 页面之间怎么跳转、共享数据 |
| 三、通信与身份认证 | Axios、PyJWT、Werkzeug | 前后端怎么说话、系统怎么知道你是谁 |
| 四、前端视觉与交互 | Tailwind CSS、Font Awesome、Leaflet、Canvas 2D | 网页长什么样、数据怎么直观展示 |
| 五、后端框架与服务运行 | FastAPI、Uvicorn | 服务器怎么处理请求 |
| 六、数据存储与访问 | PostgreSQL、PostGIS、SQLAlchemy、psycopg2、geoalchemy2、tsvector+GIN | 数据存在哪里、怎么查 |
| 七、辅助工具与服务 | pdfplumber、Pillow、DeepSeek API | 文件解析、安全防护、AI 分析 |

---

## 一、前端框架与工程化

这一层回答的问题是："用户看到的网页是怎么来的？代码怎么写、怎么跑？"

### 1. Vue（前端框架）

Vue 是一个用于构建网页用户界面的 JavaScript 前端框架。简单说，Vue 负责用户打开网页后看到的一切。

在没有 Vue 之前，网页通常使用 HTML + CSS + JavaScript 直接开发：用户点击按钮 → JavaScript 获得点击操作 → 修改 HTML 代码 → 刷新页面内容。这种方式的缺点是页面逻辑散落在不同文件中，项目大了以后很难维护。

**Vue 的两个核心思想：**

（1）**组件化开发**——把一个大网页拆成很多小模块，每个组件独立开发、可以重复利用、容易维护。比如数据统计组件、导航栏组件、台风模块组件。就像搭积木，每个积木做好自己的事，拼起来就是一个完整的页面。

（2）**响应式数据绑定**——不需要手动刷新页面。数据变了，Vue 自动更新页面上对应的内容。比如用户登录后，导航栏自动从"请登录"变成"欢迎，张三"。

**Vue 的基本开发单位**是 `.vue` 文件，一个文件包含三个部分：

```
<template>...</template>    → 网页结构（HTML）
<script>...</script>        → 页面逻辑（JavaScript）
<style>...</style>          → 页面样式（CSS）
```

一个页面的结构、逻辑、样式全部集中在一个文件里管理。

> Vue 3.5 是 MeteoScholar 的前端核心框架，通过组件化和响应式机制构建全部 24 个页面组件，涵盖登录、文献检索、台风/地震/龙卷风三大灾害分析等所有用户可见的界面。

---

### 2. Vite（构建工具）

浏览器不能直接理解 `.vue` 文件。Vite 的作用就是将 Vue 代码转换成浏览器能运行的网页代码：

```
Vue 代码 → Vite 处理 → HTML + CSS + JavaScript → 浏览器显示网页
```

现代前端项目不是直接写 `index.html`、`style.css`、`main.js`，而是使用 Vue 组件、TypeScript、JavaScript 模块、CSS 预处理、第三方库等。浏览器无法理解这种工程结构，所以需要 Vite 来完成**构建（Build）**——将开发者写的工程代码转换为浏览器最终运行的网页代码。

TypeScript = JavaScript + 类型系统 + 一些工程化增强。所有合法 JavaScript 基本都是合法 TypeScript。但是浏览器不认识TypeScript，所以需要Vite编译成JavaScript让浏览器执行

||JavaScript|TypeScript|
|---|---|---|
|文件|.js|.ts|
|浏览器直接运行|✅|❌|
|关系|基础|JS增强版|
|类型检查|弱|强|
|运行速度|一样|一样|
|适合大型项目|一般|更适合|

**热更新 HMR（Hot Module Replacement）：** 这是 Vite 最直观的功能。修改代码保存后页面自动变化，不用重新打开网页——Vite 只更新你改的那个组件，页面其余部分保持不变。传统方式则需要重新编译整个项目、刷新浏览器、手动找回刚才的页面位置。

**Vite 在项目中的四个作用：** 编译 Vue 页面；管理前端依赖（npm）；提供开发服务器（`localhost:5173`）；项目发布打包（生成 `dist` 目录部署到服务器）。

> Vite 6.0 是 MeteoScholar 前端项目的工程化核心。开发阶段通过热更新实现快速迭代，发布阶段将 Vue 单文件组件转换为浏览器可运行的静态网页资源。

---

## 二、前端状态与页面导航

这一层回答的问题是："用户登录后，各个页面怎么共享用户信息？怎么在页面之间跳转？"

### 3. Pinia（状态管理）

Pinia 是 Vue 官方推荐的状态管理库（State Management Library）。它提供一个**所有组件都可以访问的公共数据仓库**。

用户登录后，系统知道：用户名是"张三"，用户ID是10001，身份是普通用户，Token 是 xxxxxx。这些信息需要被很多页面使用——首页显示"欢迎 张三"，个人中心显示用户信息，上传文献页面判断是否登录。如果每个页面都自己保存一份，会出现数据重复、修改困难、页面之间不同步的问题。Pinia 解决的就是这个：所有页面从一个公共仓库读取，改一处所有地方都同步。

**Pinia 和数据库的区别：**

| 特性 | Pinia | PostgreSQL |
|---|---|---|
| 位置 | 浏览器前端 | 服务器后端 |
| 作用 | 保存运行状态 | 永久保存数据 |
| 生命周期 | 网页运行期间 | 长期保存 |
| 访问者 | 当前用户 | 所有用户 |
| 数据量 | 少量（用户信息、Token） | 大量（全部业务数据） |

> Pinia 2.3 类似于前端应用的"临时记忆"——页面一关就没了。它是给 Vue 页面之间共享临时数据用的，不是用来永久存数据的。永久存储靠后端的 PostgreSQL。

---

### 4. Vue Router（前端路由）

Vue Router 是 Vue 官方的路由管理库，一句话总结就是：**输入不同网址，显示不同页面**。

现实中的路由：南京 → 上海、南京 → 北京，路线不同到达不同地点。网页中的路由也一样：

```
/login     → 登录页面
/search    → 搜索页面
/typhoon   → 台风页面
```

输入不同的网络路线（网址），抵达不同的页面。

**传统网页 vs Vue 单页面应用（SPA）：**

以前的网站：每进入一个新页面，浏览器就向服务器请求新的 HTML → 下载 → 重新加载整个网页。页面刷新闪烁、体验差、状态丢失。

Vue 使用 SPA（Single Page Application，单页面应用）。整个网站实际上只有一个 `index.html`。无论访问什么路径，加载的都是同一个文件，Vue Router 根据 URL 动态替换里面的组件，**整个过程不重新加载整个页面**：

```
用户访问 → URL改变 → Vue Router监听 → 找到对应.vue组件 → 替换页面内容
```

**路由导航守卫（Navigation Guard）：** Vue Router 还可以在用户进入页面前先检查条件——这就是权限控制。访问受保护页面时先检查 Pinia 中有没有 Token：有就放行，没有就强制跳转登录页。

> Vue Router 4.5 负责 24 条前端路由的映射与切换，并通过导航守卫结合 Pinia 实现未登录用户无法访问受保护页面的权限控制。

---

## 三、前后端通信与身份认证

这一层是本系统最关键的桥梁层——回答"前端怎么跟后端说话"以及"后端怎么知道你是谁"。通信工具（Axios）和认证机制（PyJWT + Werkzeug）在实际流程中密不可分，放在一起讲。

### 5. Axios（HTTP 通信库）

Axios 是基于 JavaScript 的 HTTP 请求库，是 Vue 前端与 FastAPI 后端之间数据交流的桥梁。通信协议是 HTTP，数据格式是 **JSON**（JavaScript Object Notation，一种人和机器都容易阅读的轻量级格式）。

**Axios 的三个核心作用：** 发送 HTTP 请求（"我要查询文献"、"我要登录"）；接收后端返回的 JSON 数据交给 Vue 显示；自动处理身份认证——每次请求自动携带 JWT Token。

**请求拦截器（Request Interceptor）：** 普通流程是 `Vue → Axios → FastAPI`。加了拦截器后变成 `Vue → Axios拦截器（自动从 Pinia 读取 Token 并添加到请求头） → FastAPI`。开发者不需要在每个请求里手动加 Token。

**响应拦截器（Response Interceptor）：** Axios 也能拦截服务器返回。当服务器返回 `401 Unauthorized`（身份验证失败——Token 过期、退出登录等），拦截器自动执行：清除 Token → 清除 Pinia 用户状态 → 跳转登录页。

> Axios 1.7 是前后端通信的桥梁。请求拦截器自动携带 JWT Token 实现身份认证，响应拦截器处理登录失效自动跳转，同时Axios支持文件上传（multipart/form-data）和下载（Blob）。

---

### 6. 身份认证体系（PyJWT + Werkzeug）

现在回答"身份认证"这整件事：用户怎么登录？系统怎么确认身份？密码怎么保存？四个角色协作完成：

- **Pinia**（前端）：保存 Token——"我是谁"
- **Axios**（前端）：携带 Token——"这是我的身份证明"
- **PyJWT**（后端）：生成和验证 Token——"你的身份证明是真的还是假的"
- **Werkzeug**（后端）：哈希和验证密码——"你的密码对不对"

#### 6.1 PyJWT — JSON Web Token 认证

**JWT 是什么？** JWT（JSON Web Token）是一种无状态的身份认证令牌。登录成功后，服务器不保存你的登录状态，而是给你一个"身份证明文件"（Token）。以后每次访问你带着它，服务器验证是真的就放行。

JWT 由三部分组成，用点号分隔：`Header.Payload.Signature`

- **Header**——描述签名算法（HMAC-SHA256）
- **Payload**——保存用户信息如 `{"user_id":1, "username":"admin", "exp":24小时后}`。⚠️ Payload 不是加密的，任何人拿到 Token 都能读取，所以绝对不能存密码
- **Signature**——防篡改签名，由 `Header + Payload + SECRET_KEY` 通过 HMAC-SHA256 生成。只要 SECRET_KEY 不泄露，攻击者无法伪造

**JWT vs 传统 Session：**

| 维度 | Session | JWT |
|---|---|---|
| 状态保存位置 | 服务器（需记住每个用户） | 客户端（服务器不保存） |
| 服务器压力 | 需保存大量 Session | 无需保存 |
| 扩展多个服务器 | 需共享 Session | 天然支持 |
| 适合前后端分离 | 一般 | 优秀 |

Token 24 小时后自动过期。到期后后端返回 401 → Axios 响应拦截器自动清除 Token → 跳转登录页。

> PyJWT 2.10 负责按 JWT 标准（RFC 7519）生成和验证身份令牌。登录时用 SECRET_KEY 签名生成 Token，后续请求通过验证签名和有效期确认身份。

#### 6.2 Werkzeug — 密码哈希

**为什么密码不能直接保存？** 数据库被泄露时，明文密码会让所有用户账号暴露。密码必须经过**哈希（Hash）**处理后存储。

**哈希不是加密。** 加密可以解密（密码 → 密文 → 密钥解密 → 密码），哈希是单向不可逆的（密码 → 哈希值，没有"解密钥匙"）。数据库里存的是哈希值，泄露了也无法反推原始密码。

本项目使用 Werkzeug 的两个函数：`generate_password_hash()` 将密码转为哈希存入数据库；`check_password_hash()` 验证时不是"解密"，而是把用户输入重新算一遍哈希，和数据库比对，一致就通过。

**scrypt 算法 + 随机盐值（salt）：** scrypt 故意增加内存和 CPU 计算量让暴力破解成本极高。盐值是在哈希前给每个密码加一段随机字符串——即使两个用户密码完全相同（都是 `123456`），由于盐值不同，最终存储的哈希值也完全不同。

> Werkzeug 3.0+ 提供密码安全存储能力。v6.1 后端从 Flask 迁移到 FastAPI，但为保持与 v5.0 用户数据兼容，继续沿用 Werkzeug 密码哈希方案。

#### 6.3 完整认证流程

把四个工具串起来，就是一次完整的用户登录与后续访问：

```
【登录阶段】
用户输入账号密码 → Vue 发送 POST /login（Axios）
    → FastAPI → 查询 PostgreSQL users 表
    → Werkzeug.check_password_hash() 验证密码
    → PyJWT 生成 Token（user_id + username + 过期时间）
    → 返回 Token → Pinia 保存 + localStorage 持久化

【后续访问阶段】
访问任何页面 → Axios 请求拦截器从 Pinia 读取 Token
    → 自动添加 Authorization: Bearer <token> 请求头
    → FastAPI → PyJWT 验证签名和过期时间
    → 通过 → 返回数据
    → 失败 (401) → Axios 响应拦截器清除 Token → 跳转 /login

【页面权限控制】
Vue Router beforeEach() → 检查 Pinia 中有无 Token
    → 有 → 进入页面
    → 无 → 强制跳转 /login
```

---

## 四、前端视觉与交互

这一层回答"网页长什么样"和"数据怎么直观展示"。

### 7. Tailwind CSS（样式框架）

Tailwind CSS 是一个 CSS 样式框架，负责颜色、字体、间距、布局、边框、圆角、阴影、动画。

HTML 负责结构：`<button>搜索</button>`。浏览器知道这是个按钮，但不知道它应该是什么颜色、多大、有没有圆角。CSS（层叠样式表）的作用就是告诉浏览器"这个按钮应该长什么样"。

Tailwind 采用 **Utility-first（实用工具优先）** 的设计理念：不提前写大量 CSS 类，而是直接在模板中组合预定义的小型样式工具类（如 `class="flex items-center p-4 rounded-xl shadow-md"`）。Tailwind 本质还是生成 CSS，最终浏览器看到的仍然是 HTML + CSS + JavaScript。

**CDN 加载：** CDN（Content Delivery Network，内容分发网络）是全球多个服务器存放同一份资源，类似在线看视频——文件在远端服务器，通过 CDN 加载无需下载到项目，在 `.vue` 文件中直接使用。

> Tailwind CSS 是 MeteoScholar 前端视觉设计的核心样式框架。相比固定组件库（如 Element Plus），Tailwind 提供更高的界面定制自由度，使系统能根据气象科研平台的需求设计复杂的信息卡片、分析面板和可视化页面。

---

### 8. Font Awesome（图标库）

Font Awesome 是一个开源矢量图标库——一套设计好的小图标，给按钮、菜单和功能模块加上直观的视觉标识。

没有图标：`[ 搜索 ]`——需要读文字才能理解。有图标：`[ 🔍 搜索 ]`——一眼就知道功能。

Font Awesome 的图标不是普通图片。传统图片（`search.png`）由像素组成，放大就模糊。而 Font Awesome 使用 **SVG / 字体矢量技术**，无论在小按钮还是大屏幕上，始终清晰不模糊。

> Font Awesome 4.7 通过 CDN 加载，为系统中登录、搜索、文件管理、灾害分析、地图和 AI 功能提供统一图标。它不参与业务逻辑，只负责增强界面可读性和美观性。

---

### 9. Leaflet（地图引擎）

Leaflet 是一个轻量级开源 JavaScript 地图库，帮助网页显示交互式地图，将经纬度数据转换成地图上的点、线、区域。

> ⚠️ 核心区分：Leaflet ≠ 地图数据，≠ 高德地图/OSM，≠ 数据库。Leaflet 只是一个"地图显示和交互引擎"。

**为什么需要地图引擎？** 普通网页只能显示文字"台风中心：121.5°E, 18.2°N"，但用户需要直观看到台风在哪。Leaflet 负责把坐标变成可视化的地图图形。

**Leaflet 和地图数据的关系：** 它本身不保存任何地图——`地图数据源 → Leaflet → 浏览器显示地图`。

**核心机制——地图瓦片（Tile）：** 现代网页地图不是一整张图片，而是切成大量小块。用户看南京时 Leaflet 只加载南京附近的瓦片，速度极快。

**经纬度到屏幕的转换：** 地球用经纬度坐标，屏幕用像素坐标（如 x=800, y=500）。Leaflet 通过地图投影算法（默认 Web Mercator）将球面坐标转换为平面像素坐标。

**Leaflet 与 ECharts 的分工：**

| 特性 | Leaflet | ECharts |
|---|---|---|
| 核心问题 | 在哪里 | 多少、趋势 |
| 数据类型 | 空间坐标 | 统计数据 |
| 显示内容 | 地图、路径、点 | 折线、柱状、饼图 |

**在 MeteoScholar 中的作用：** 台风路径可视化（`PostgreSQL → FastAPI → Vue → Leaflet → 地图显示`）；地震震中展示；龙卷风位置展示。

> Leaflet 1.9 是空间可视化的核心地图引擎，将数据库坐标转换为地图上的点、线、区域，实现台风路径追踪、地震震中和龙卷风空间分布分析，并结合 Canvas 2D 实现动态气象可视化。

---

### 10. Canvas 2D（浏览器原生绘图）

Canvas 是 HTML5 提供的浏览器原生二维绘图技术。创建一块 `<canvas></canvas>` 画布，然后用 JavaScript 在上面绘制：画一个点 → 画一条线 → 写文字 → 画一个圆。最终形成图表、动画、数据可视化效果。

Canvas 的本质是**一个可以操作像素的区域**。普通图片是 `image.jpg → 浏览器读取 → 显示`，而 Canvas 是 `JavaScript → 实时计算 → 绘制像素 → 显示`——它是**动态生成的图像**，自由度极高。

**Canvas 与其他技术的关系：**

```
Canvas    → 底层绘图技术（浏览器原生，自由度高但开发成本也高）
ECharts   → 基于 Canvas/SVG 封装的上层图表库（现成的折线图、柱状图等）

Leaflet → 负责地图、坐标系统、缩放等交互功能
Canvas  → 负责动态绘制、粒子、动画、数据效果
```

例如台风模拟：Leaflet 显示中国地图和台风路径，Canvas 在上面叠加风场粒子、移动轨迹、动态效果。本项目选 Canvas 自绘是因为绘制类型固定（地震趋势图、灾害统计图、台风强度时序图），且需要自定义动画效果与 Leaflet 地图叠加，Canvas 在此场景下提供了最高的控制精度。对于通用的统计图表，成熟的图表库（如 ECharts）通常开发效率更高。

> Canvas 2D 允许 JavaScript 直接控制像素进行高性能绘制，相比 SVG 的 DOM 节点模式更适合大量数据点。MeteoScholar 用 Canvas 自绘地震趋势图、灾害统计图和气象强度变化图，并与 Leaflet 结合实现复杂气象数据可视化。

---

## 五、后端框架与服务运行

这一层回答"服务器收到请求后怎么处理？怎么把程序跑起来？"

### 11. FastAPI（后端 Web 框架）

FastAPI 是基于 Python 的现代化 Web 后端框架，用于快速构建高性能 API 服务。

直接用 Python 写后端需要处理网络连接、HTTP 协议、请求解析、数据返回等底层事务，非常繁琐。Web 框架提供封装好的工具，让开发者专注业务逻辑。

**什么是 API？** API（Application Programming Interface，应用程序接口）就是**一个程序向另一个程序开放的“功能入口”和“调用规则”**，以便给其调用的接口。前端 Vue 和后端 Python 是两个不同的程序，API 是它们约定的交流方式。其通常表现为：一个地址+一套规则。

| 概念           | 作用          | 你的项目例子        |
| ------------ | ----------- | ------------- |
| IP地址         | 找到哪台机器      | localhost     |
| 端口 Port      | 找到机器上的哪个程序  | 5000（FastAPI） |
| 接口 Interface | 程序提供的功能入口   | login函数       |
| API          | 程序之间调用的接口   | POST /login   |
| HTTP         | 通信规则        | GET、POST      |
| Axios        | 发送HTTP请求的工具 | Vue调用FastAPI  |

概念通俗理解：
```
API：门口地址和办事规则
HTTP：和门卫交流的语言规则（简体中文）
Axios：帮你去门口和门卫交流的人
```

**REST API：** REST 是一种接口设计规范，用 HTTP 方法表达操作意图：
	
| 方法 | 作用 | 例子 |
|---|---|---|
| GET | 获取数据 | `GET /api/documents` 查询文献 |
| POST | 提交数据 | `POST /login` 登录、`POST /api/documents/upload` 上传 |
| PUT | 修改数据 | 修改用户信息 |
| DELETE | 删除数据 | `DELETE /api/documents/{id}` 删除文献 |

**FastAPI 的五个核心：**

**（1）ASGI 原生异步——与 Flask 最大的区别。** WSGI（传统，Flask 用）：请求A → 处理完 → 请求B，一个请求占住一条线。ASGI（现代，FastAPI 用）：请求A 等数据库时，服务器可同时处理请求B，不阻塞。对于 AI 调用、大文件上传等场景优势明显。

| 方面 | Flask（v5.0） | FastAPI（v6.1） |
|---|---|---|
| 通信标准 | WSGI（同步） | ASGI（异步） |
| 参数验证 | 手动写代码 | Pydantic 自动 |
| API 文档 | 需插件 | 自动生成 Swagger |
| 性能 | 中等 | 更高 |
**（2）Swagger（API 文档与在线测试工具）:**  是一套用于描述、展示和测试WEB API的工具规范。在FastAPI中，Swagger主要表现为一个自动生成的API在线文档页面，让开发者可以直接查看接口、填写参数、发送请求、测试返回结果。Swagger = API 的说明书 + 在线测试工具。OpenAPI是API的标准描述格式。Swagger可以直接发送HTTP请求，直接调用API。
  
**（3）依赖注入（Depends）：** 很多接口都需要验证登录。Depends 把公共功能自动注入到接口中——访问接口前自动执行，业务代码无需手动调用。

**（4）Pydantic 数据验证：** 自动检查数据类型。前端传的"年龄"应该是数字，如果传了字符串"abc"，Pydantic 自动拦截并返回错误，不需要手写检查代码。

**（5）自动生成 API 文档：** 访问 `http://localhost:5000/docs` 即能看到所有接口说明，并且**可以直接在页面上点击测试**，无需额外安装 Postman。

> FastAPI 0.115 是 MeteoScholar 6.1 后端核心 API 框架，10 个路由模块约 40 个 API 端点全部由 FastAPI 承载——接收前端请求、校验参数、查询数据库、调用 DeepSeek AI、返回 JSON 响应。

---

### 12. Uvicorn（ASGI 服务器）

Uvicorn 是基于 ASGI 标准的高性能 Python Web 服务器。二者的分工很清楚：

- **FastAPI**：负责"这个请求应该执行什么代码？返回什么数据？"（业务逻辑）
- **Uvicorn**：负责"请求从哪里来？如何接收？如何发送出去？"（网络通信）

当用户访问 `http://localhost:5000/docs`：浏览器发送请求 → Uvicorn 监听 5000 端口接收 → 交给 FastAPI 处理 → 结果沿原路返回。

**什么是 ASGI 标准？** 类似手机充电：以前不同手机有不同接口，后来 USB 统一了。ASGI 就是 Python Web 服务器之间的统一通信标准，规定了服务器和应用之间如何传递请求、响应和异步任务。传统 Flask 用 WSGI 服务器，FastAPI 用 ASGI 服务器（Uvicorn），这是两者架构上的本质区别。

**启动命令：** `uvicorn main:app --reload --port 5000`——启动 Uvicorn，找到 `main.py` 中的 `app = FastAPI()` 应用，监听 5000 端口，开启开发模式自动重载。

**职责边界：**

| 功能 | FastAPI | Uvicorn |
|---|---|---|
| 定义接口、业务逻辑、参数验证、数据库操作 | ✅ | ❌ |
| 监听端口、接收请求、网络连接、启动服务器 | ❌ | ✅ |

> Uvicorn 0.30 负责启动 FastAPI 应用、监听 5000 端口、接收 HTTP 请求并转发给 FastAPI 处理。FastAPI 决定"做什么"，Uvicorn 负责"让请求能进来"。

---

## 六、数据存储与访问

这一层是系统的"地基"——回答"数据存在哪里、怎么存、怎么查"。

### 13. PostgreSQL（关系型数据库）

PostgreSQL 是功能完善的开源对象关系型数据库，在科研数据分析、企业级系统和 GIS 应用领域具有广泛应用。17 张表的所有数据——用户、文献全文、台风路径、地震参数、AI 分析缓存等——全部存在这里。

**为什么叫"对象关系型"？** 传统数据库只能存数字、字符串、日期。PostgreSQL 支持更高级的数据类型：JSONB（半结构化数据）、ARRAY（数组）、tsvector（全文搜索向量）、空间数据（通过 PostGIS）。

**全文检索能力：** 传统 `LIKE '%人工智能%'` 需要数据库逐行扫描——100 万篇论文就要检查 100 万次。PostgreSQL 用 **tsvector + GIN 索引**解决：tsvector 把文章转成关键词集合（如"人工智能技术应用于气象预测"→ `{人工智能, 技术, 气象, 预测}`），GIN 倒排索引类似书籍末尾的关键词目录——直接查"人工智能 → 第10页、第50页、第200页"，无需逐页翻找。

**PostgreSQL vs MySQL：**

| 方面 | MySQL | PostgreSQL |
|---|---|---|
| 定位 | 互联网业务 | 科研/企业级 |
| 空间能力 | 基础 | PostGIS 强大 |
| 全文检索 | FULLTEXT | tsvector + GIN |
| 扩展能力 | 一般 | 非常强 |

> PostgreSQL 15+ 在空间分析、全文检索、复杂科研查询和高级数据类型方面具有明显优势，适合作为气象科研数据平台的数据库基础。

---

### 14. PostGIS（空间数据库扩展）

PostGIS 是 PostgreSQL 的空间数据库扩展，给数据库增加了处理地理空间数据的能力。

**普通经纬度 vs PostGIS 空间对象：** 普通数据库存了 `longitude=118.78, latitude=32.04`，对它来说这只是两个数字——它不知道这是南京、不知道两个点距离多少、不知道哪些灾害在附近。安装 PostGIS 后，经纬度被转换为 `POINT(118.78 32.04)`，数据库就知道这是地球上的一个位置，于是可以计算距离、方位、是否附近、是否包含。

**三种空间数据类型：** Point（点，如地震震中）、LineString（线，如台风路径）、Polygon（面，如区域范围）。

**geometry vs geography：** geometry 用平面坐标算，速度快但单位是度数（0.5° 不是 500 公里）；geography 用真实地球表面算，单位是米，本项目距离计算统一用 geography。

**空间索引 GIST：** 普通查询"距离南京 500 公里以内的所有地震点"，如果没有 GIST 索引需要扫描 100 万个点逐个算距离；有 GIST 则先快速定位附近区域，只对少量候选点算精确距离。

**自动维护——触发器：** 数据库同时保存经纬度和空间对象。触发器（Trigger）是数据库自动执行的小程序——写入经纬度时自动生成对应的 geometry 字段，无需业务代码手动操作。

> PostGIS 3.x 将普通经纬度升级为具有空间意义的对象，通过 GIST 索引和 ST_DWithin、ST_Distance 等函数实现距离计算和邻近搜索，支撑台风路径、地震震中、龙卷风位置等核心空间可视化功能。

---

### 15. SQLAlchemy（对象关系映射框架）

SQLAlchemy 是 Python 中最成熟的 ORM（Object Relational Mapping，对象关系映射）框架。它把数据库表映射为 Python 类，让开发者用面向对象的方式操作数据库。

**三个核心组成：** Engine（数据库引擎，管理连接池和网络通信）；Session（数据库会话，一次操作过程——打开→执行SQL→返回结果→关闭）；Model（模型，描述表结构，每个 Python 类对应一张表）。

**本项目的混合模式——ORM + 原生 SQL：** `models/__init__.py` 定义了 17 张表的 ORM 模型，但路由中的实际查询全部写原生 SQL。因为本项目的空间查询（`ST_DWithin`）、全文检索（`plainto_tsquery`）、复杂聚合统计若用 ORM 表达会极其冗长。原始 SQL 更清晰、更好优化。SQLAlchemy 的核心价值在于**引擎管理、连接池和会话生命周期管理**。

| 方面 | ORM | 原生 SQL |
|---|---|---|
| 简单增删改查 | 优秀 | 一般 |
| 复杂查询 | 较弱 | 优秀 |
| 数据库特性利用 | 有限 | 充分 |

**连接池：** 没有连接池时每次请求都要重新连接→查询→关闭，效率极低。有连接池后提前准备多个连接待命，请求来了直接取用，适合多用户并发。

> SQLAlchemy 2.0 通过 ORM 模型定义数据库结构、通过 Engine 和 Session 管理连接与事务。本项目采用混合模式：用 SQLAlchemy 管理数据库生命周期，针对复杂查询直接编写原生 SQL。

---

### 16. psycopg2（PostgreSQL 数据库驱动）

psycopg2 是 Python 中最成熟的 PostgreSQL 数据库驱动，负责 Python 程序与 PostgreSQL 之间的**底层网络通信**。

**为什么需要驱动？** 数据库只理解 SQL 协议，不理解 Python。驱动就是翻译官——把 Python 指令转换为数据库能理解的通信。

**psycopg2 负责：** 建立数据库连接（通过地址、端口 5432、用户名密码）；发送 SQL 语句；接收查询结果并转为 Python 对象；处理事务（保证操作要么全成功要么全回滚）。

**后端访问数据库的完整层级：**

| 层 | 作用 |
|---|---|
| FastAPI | 处理 HTTP 请求和业务逻辑 |
| SQLAlchemy | ORM 抽象、Session 管理 |
| psycopg2 | 底层通信（Python ↔ PostgreSQL） |
| PostgreSQL | 真正存储和查询数据 |

**为什么 SQLAlchemy 不直接连接数据库？** 因为数据库种类很多（PostgreSQL、MySQL、SQLite、Oracle），每种协议不同。SQLAlchemy 提供统一接口——代码写 `session.query(User)`，不关心下面是什么数据库。真正连接由各数据库的驱动完成：PostgreSQL → psycopg2，MySQL → pymysql，SQLite → sqlite3。

```
SQLAlchemy（统一接口） → 数据库驱动（psycopg2） → 具体数据库（PostgreSQL）
```

> psycopg2 2.9 负责 Python 与 PostgreSQL 的底层网络通信。SQLAlchemy 提供高层抽象，psycopg2 负责底层执行，二者共同组成完整的数据访问链路。

---

### 17. geoalchemy2（PostGIS ORM 适配）

geoalchemy2 是 SQLAlchemy 的 PostGIS 空间扩展适配器，让 SQLAlchemy 能定义和操作 PostGIS 的空间数据类型。

普通 SQLAlchemy 能理解整数、字符串、日期，但它不知道 `POINT(118.78 32.04)` 是什么。geoalchemy2 让它理解"这是一个地理坐标点、这是一个轨迹线"。

**geoalchemy2 不是 GIS 引擎！** 它不会计算距离、不会分析空间关系、不会渲染地图。它只是在 Python 对象和 PostGIS 字段之间做类型转换。

空间架构的三层分工：

```
geoalchemy2  → "认识空间数据"（Python ↔ PostGIS 类型转换）
PostGIS      → "计算空间关系"（距离、邻近搜索、空间分析）
Leaflet      → "展示空间结果"（地图渲染、用户交互）
```

| 功能 | SQLAlchemy | geoalchemy2 |
|---|---|---|
| 普通字段（字符串、数字） | ✅ | ❌ |
| Point、LineString、Polygon | ❌ | ✅ |
| PostGIS 完整兼容 | 部分 | 完整 |

> geoalchemy2 0.15 是 Python ORM 与 PostGIS 之间的空间数据适配层。真正的距离计算和空间分析仍由 PostGIS 完成。

---

### 18. PostgreSQL tsvector + GIN（全文检索详解）

在 PostgreSQL 概述（第 13 节）中已介绍了全文检索的基本概念，这里补充技术细节。

**核心两部分：** ① tsvector（文本搜索向量）将文本转为关键词集合，保存的是 **Lexeme（词位）**——经过规范化处理的最小搜索单位，如英文 `running` → `run`，搜索 `run` 也能找到 `running`。② GIN 索引（广义倒排索引）建立关键词→文档的快速映射，类似书籍末尾的关键词目录。

**为什么用 `simple` 分词配置？** PostgreSQL 默认的 `english` 会做英文词干提取，但本系统面向中文文献：中文没有词干变化，`english` 可能导致中文单字被丢弃；`simple` 按空格和标点切分，与 ILIKE 兜底策略兼容最好，且不做词干提取，精确匹配更可靠。

**权重设计：** 标题 A > 摘要 B > 全文 C，通过触发器自动维护，论文新增或修改时自动重建 tsvector。

**混合搜索策略——双通道保障：**

```sql
WHERE (
  full_text_tsv @@ plainto_tsquery('simple', :kw)  -- tsvector 主搜索，走 GIN 索引
  OR full_text ILIKE '%kw%'                         -- ILIKE 兜底，精确包含匹配
)
ORDER BY ts_rank_cd(full_text_tsv, query) DESC      -- 相关性排序
```

为什么要 ILIKE 兜底？中文没有天然空格，分词结果不一定符合预期。ILIKE 直接判断字符串是否包含目标词，即使 tsvector 没识别出来也能找到，保证了检索的召回率。

> tsvector + GIN 是 MeteoScholar 文献检索的核心引擎。采用 simple 中文兼容配置，标题/摘要/全文三级权重排序，结合 ILIKE 中文兜底，在速度和准确性之间取得平衡。

---

## 七、辅助工具与服务

这一层覆盖支撑系统运行的三个辅助工具：文献 PDF 解析、登录安全验证码、AI 智能分析。

### 19. pdfplumber（PDF 文本提取）

pdfplumber 是 Python 的 PDF 内容解析库，能从 PDF 文件中提取文本、表格、字符坐标和元数据。它基于 PDF 内部结构解析，而非 OCR 图像识别。

简单理解：pdfplumber 是**连接"PDF 文件"和"数据库文本字段"的转换工具**。

PDF 内部不是 Word 那样的连续文本，而是由页面对象、文字对象、坐标位置、字体信息组成——每段文字都有自己的坐标。pdfplumber 读取这些对象并重新组合成普通文本。

**pdfplumber vs OCR：**

| 特性 | pdfplumber | OCR |
|---|---|---|
| 适用场景 | 文字型 PDF | 扫描版 PDF（图片） |
| 速度 | 快 | 慢 |
| 准确率 | 高 | 依赖模型 |
| 是否需要 AI | 否 | 是 |

> pdfplumber 0.11 负责将用户上传的 PDF 文献转为纯文本存入 `documents.full_text` 字段。随后 PostgreSQL 触发器自动生成 tsvector 检索向量并建立 GIN 索引，使上传的 PDF 不仅能下载查看，还能参与系统级全文检索。

---

### 20. Pillow（验证码图片生成）

Pillow 是 Python 最常用的图像处理库（PIL 的现代分支），提供图片创建、绘制、格式转换、像素处理等功能。本项目用它动态生成登录验证码。

**为什么需要验证码？** 没有验证码时，攻击者可编写脚本不断尝试 `admin/123456`、`admin/password`、`admin/abc123`……这叫**暴力破解（Brute Force Attack）**。验证码的作用就是让自动化脚本无法批量尝试。

**生成与验证流程：**

```
用户访问登录页 → 后端生成随机字符 → Pillow 创建图片（随机背景+干扰线+噪点+字符偏移）
    → 返回 PNG 图片 → 用户输入 → 后端比对验证
```

**验证码存储——为什么用 HttpOnly Cookie？** 普通方式用 `localStorage` 存验证码，如果发生 XSS 攻击，脚本可以读取。HttpOnly Cookie 由浏览器保存但 JavaScript 无法访问，安全性更高。验证码有效期 5 分钟。

> Pillow 10.4 动态生成 160×60 像素的 PNG 验证码图片（随机浅色背景、5 条干扰线、100 个噪点、4 位随机字符各有偏移和颜色），结合 HttpOnly Cookie 存储验证码状态，与 Werkzeug 密码哈希和 PyJWT Token 共同组成系统安全认证链路。

---

### 21. DeepSeek API（大语言模型 AI 分析）

DeepSeek API 是由 DeepSeek 提供的大语言模型服务接口，允许应用程序通过 HTTP 请求调用大模型能力实现自然语言理解和专业领域分析。

**调用方式——标准 HTTP API：**

```
FastAPI 后端 → HTTP POST 请求 → DeepSeek API → 大模型推理 → 返回 JSON 结果
    → 保存到数据库缓存表 → 返回前端展示
```

**请求结构：** 调用 `/chat/completions` 端点，发送两个关键 prompt：

- **system prompt**（角色设定）：`"你是一名气象灾害分析专家，请根据提供的数据进行科学解释。"`
- **user prompt**（具体数据）：台风名称、中心气压、最大风速、移动方向、历史路径等结构化参数

**两个关键参数：**

- `temperature=0.3`（范围 0~1）：控制输出随机性。灾害分析需要稳定准确而非创意发挥，所以设为较低值
- `max_tokens=350`（约 100~150 中文字）：限制生成长度，避免输出过长、浪费 API 额度、页面显示困难

**AI 结果缓存机制：** 首次请求 → 调用 DeepSeek → 生成结果 → 保存到 `*_ai_analysis` 缓存表。后续相同请求 → 直接读缓存返回，不消耗 API 额度。形成"数据库缓存 + AI 生成"的混合架构。

> ⚠️ 定位说明：AI 分析属于基于已有数据的辅助科学解释，不等同于专业气象预测模型。其输出应作为科研参考，而非确定性预报结论。

> DeepSeek API（deepseek-chat 模型）是 MeteoScholar 智能分析模块的核心，将台风（4 种分析）、地震（3 种）、龙卷风（3 种）的结构化灾害数据转换为自然语言科学解读。通过 Prompt 工程控制分析方向，通过数据库缓存减少重复 API 调用。

---

## 附录：技术依赖加载方式

不同技术运行在不同环境，加载方式也不同。MeteoScholar v6.1 涉及六种依赖加载方式：

### npm（Node Package Manager）

用于前端 JavaScript 生态依赖。涉及 Vue 3.5、Vite 6.0、Pinia、Vue Router、Axios。流程：`npm install` → 下载到 `frontend/node_modules/` → Vite 读取并编译 → 生成浏览器可运行文件。优点：版本可控、自动管理依赖关系。缺点：`node_modules` 体积较大，需要 Node.js 环境。

### pip（Python Package Installer）

用于 Python 后端依赖。涉及 FastAPI、Uvicorn、SQLAlchemy、psycopg2、pdfplumber、Pillow、PyJWT。流程：`pip install` → 安装到 Python 环境的 `site-packages/` → FastAPI 启动时通过 `import` 加载。Python 程序运行时按 `import xxx` 寻找已安装的库。

### CDN（内容分发网络）

直接通过网络从远程服务器加载前端资源。涉及 Tailwind CSS、Font Awesome。流程：浏览器打开网页 → 读取 HTML 中的 CDN 地址 → 向远程服务器请求资源 → 加载 CSS/JS。优点：无需下载安装、减小项目体积。缺点：依赖网络连接，离线环境无法加载。

### 浏览器原生支持

浏览器自身提供的标准能力，无需任何安装。涉及 Canvas 2D——它是 HTML5 标准的一部分，所有现代浏览器内置支持。流程：Vue 调用 Canvas API → 浏览器内置渲染引擎 → 生成图像。

### 系统安装 / 数据库扩展

需在操作系统或数据库服务器层面安装。涉及 PostgreSQL、PostGIS。流程：安装 PostgreSQL → 安装 PostGIS 扩展 → 在数据库中执行 `CREATE EXTENSION postgis;` → 获得空间数据能力。这类依赖不是代码文件，而是增强数据库或系统能力。

### HTTP API 调用

通过网络调用外部在线服务。涉及 DeepSeek API。流程：FastAPI → HTTP POST 请求 → DeepSeek 服务器 → AI 模型计算 → 返回 JSON。本地项目不保存模型，只通过网络调用能力。

### 加载方式总表

| 加载方式 | 代表技术 | 运行位置 |
|---|---|---|
| npm | Vue、Vite、Axios | 前端项目 |
| pip | FastAPI、Pillow、pdfplumber | Python 环境 |
| CDN | Tailwind CSS、Font Awesome | 浏览器网络加载 |
| 浏览器原生 | Canvas 2D | 用户浏览器 |
| 系统/数据库安装 | PostgreSQL、PostGIS | 服务器/数据库 |
| HTTP API | DeepSeek | 远程 AI 服务 |

> MeteoScholar v6.1 并非所有技术都以相同方式加载。前端依赖通过 npm 管理并由 Vite 构建；后端依赖通过 pip 安装并由 Python 运行时加载；部分视觉资源通过 CDN 在线获取；Canvas 由浏览器原生提供；PostGIS 等扩展需数据库层安装；DeepSeek AI 通过 HTTP API 调用外部服务。六种加载方式共同组成完整的软件运行环境。

---

## 技术栈完整清单（22 项）

| 序号 | 技术 | 类别 | 版本 | 加载方式 |
|---|---|---|---|---|
| 1 | Vue | 前端框架 | 3.5 | npm |
| 2 | Vite | 前端构建 | 6.0 | npm |
| 3 | Pinia | 状态管理 | 2.3 | npm |
| 4 | Vue Router | 前端路由 | 4.5 | npm |
| 5 | Axios | HTTP 通信 | 1.7 | npm |
| 6 | PyJWT | JWT 认证 | 2.10 | pip |
| 7 | Werkzeug | 密码哈希 | 3.0+ | pip |
| 8 | Tailwind CSS | 样式框架 | CDN | CDN |
| 9 | Font Awesome | 图标库 | 4.7 | CDN |
| 10 | Leaflet | Web 地图 | 1.9 | npm |
| 11 | Canvas 2D | 浏览器绘图 | 内置 | 浏览器原生 |
| 12 | FastAPI | 后端框架 | 0.115 | pip |
| 13 | Uvicorn | ASGI 服务器 | 0.30 | pip |
| 14 | PostgreSQL | 关系数据库 | 15+ | 系统安装 |
| 15 | PostGIS | 空间扩展 | 3.x | PostgreSQL 扩展 |
| 16 | SQLAlchemy | ORM 框架 | 2.0 | pip |
| 17 | psycopg2 | PG 驱动 | 2.9 | pip |
| 18 | geoalchemy2 | PostGIS ORM 适配 | 0.15 | pip |
| 19 | tsvector + GIN | 全文检索 | PG 内置 | PostgreSQL 内置 |
| 20 | pdfplumber | PDF 提取 | 0.11 | pip |
| 21 | Pillow | 验证码生成 | 10.4 | pip |
| 22 | DeepSeek API | AI 大模型 | deepseek-chat | HTTP API |

---

> **文档版本**：v2.0（决定版） | **对应项目版本**：MeteoScholar v6.1
>
> © 2026 南京农业大学 · 信息与计算科学专业 · 丁俊杰
