# 气象科学研究数据平台 — 技术栈说明 v6.1

> **版本**：v1.0（对应项目 v6.1.0）  
> **更新日期**：2026-07-28  
> **作者**：丁俊杰（南京农业大学，hainuo@stu.njau.edu.cn）  
> **用途**：面向初学者完整讲解本项目的全部技术选型——每一项技术是什么、为什么选它、在本项目中起什么作用

---

## 总览

本项目（气象科学研究数据平台 v6.1）采用前后端分离架构，共涉及 22 项核心技术，覆盖前端界面、后端服务、数据库存储、空间分析、人工智能和安全认证六个层面。

```
浏览器 (localhost:5173)
    ↓
Vite 开发服务器 → Vue 3 SPA
    ↓ Axios + JWT Token
Uvicorn ASGI Server → FastAPI
    ↓ SQLAlchemy + psycopg2
PostgreSQL + PostGIS (localhost:5432)
    ↓ requests
DeepSeek API（AI 分析）
```

---

## 一、前端框架层

### 1. Vue 3.5（前端框架）

**是什么**：Vue 是一个用于构建网页用户界面的 JavaScript 前端框架，采用 Composition API（组合式 API）和单文件组件（`.vue` 文件）的开发模式。

**核心机制**：响应式数据绑定——当数据变化时，页面自动更新，无需手动刷新 DOM。组件化——将大型页面拆分为独立、可复用的组件模块。

**在本项目中的作用**：构建全部 24 个页面组件，从登录注册、文献搜索到台风地震龙卷风三大灾害模块，所有用户可见的界面均由 Vue 3 渲染。

---

### 2. Vite 6.0（前端构建工具）

**是什么**：Vite 是一个基于浏览器原生 ES Module 的前端构建工具，负责开发环境的模块编译和生产环境的代码打包。

**核心机制**：开发模式下利用浏览器原生 ES Module 实现毫秒级热更新（HMR），无需完整重新编译整个项目。生产模式下基于 Rollup 打包。

**在本项目中的作用**：将 `.vue` 单文件组件编译为浏览器可执行的 HTML/CSS/JavaScript。修改任何一个 Vue 文件保存后页面即时刷新。

---

### 3. Pinia 2.3（状态管理）

**是什么**：Pinia 是 Vue 官方推荐的状态管理库，用于在前端应用中集中存储和管理跨组件共享的数据。

**与本项目的关系**：Pinia 存储的是浏览器端运行期间的状态数据（如当前登录用户信息、JWT Token），不是 MySQL/PostgreSQL 那样的持久化数据库。页面关闭后状态消失。

**在本项目中的作用**：

- `auth.js`：管理登录状态（token、用户名、用户 ID），提供 `login()`、`logout()` 接口
- `flash.js`：管理全局消息提示（成功/错误/警告，4 秒自动消失）

---

### 4. Vue Router 4.5（前端路由）

**是什么**：Vue Router 是 Vue 官方的前端路由管理库，用于控制单页面应用（SPA）中不同页面组件的切换。

**核心机制**：所有页面共享同一个 `index.html`，URL 变化时不重新加载整个网页，而是动态替换页面组件。同时支持导航守卫（`beforeEach`），在页面跳转前自动检查登录状态。

**在本项目中的作用**：管理 24 条前端路由（`/login`→`LoginView.vue`、`/typhoon`→`TyphoonView.vue` 等），并实现权限控制——未登录用户无法访问受保护页面，已登录用户不会被反复要求登录。

---

### 5. Axios 1.7（HTTP 客户端）

**是什么**：Axios 是一个基于 Promise 的 JavaScript HTTP 请求库，用于前端与后端之间的网络数据通信。通信协议为 HTTP，数据格式为 JSON。

**核心机制**：请求拦截器——每次发送请求前自动附加 JWT Token 到 `Authorization: Bearer` 请求头。响应拦截器——收到 401 状态码时自动清除本地登录状态并重定向到登录页。

**在本项目中的作用**：Vue 前端通过 Axios 向 FastAPI 后端发送所有数据请求（搜索文献、查询台风、上传 PDF、AI 分析等），并接收后端返回的 JSON 响应。

---

## 二、前端视觉与展示层

### 6. Tailwind CSS（样式框架，CDN 加载）

**是什么**：Tailwind CSS 是一个实用优先（Utility-first）的 CSS 框架，将常用布局、颜色、间距、圆角、阴影等样式封装为大量小型 CSS 工具类。通过 CDN（内容分发网络）直接在线加载，无需下载安装到项目。

**核心机制**：不写自定义 CSS 类，而是在 HTML/Vue 模板中直接组合预定义工具类（如 `class="flex items-center p-4 rounded-xl shadow-md"`）。

**在本项目中的作用**：为全部 24 个 Vue 页面提供统一的视觉风格——信息卡片、筛选面板、分页按钮、导航栏、表单等。选择 Tailwind 而非 Element Plus 等组件库的原因是：科研数据可视化平台需要高度定制化的界面设计，Tailwind 提供了这种自由度。

---

### 7. Font Awesome 4.7（图标库，CDN 加载）

**是什么**：Font Awesome 是一个提供数千个矢量图标的开源图标库。通过 CDN 在线加载，使用时只需在 HTML 中写 `<i class="fa fa-xxx">` 即可。

**在本项目中的作用**：为登录、搜索、上传、收藏、删除、台风、地震、龙卷风、地图、AI 分析等功能提供统一风格的图标标识。

---

### 8. Leaflet 1.9（Web 地图引擎）

**是什么**：Leaflet 是一个轻量级开源 JavaScript 地图库，用于在网页中显示交互式地图。Leaflet 本身不存储地图数据，它负责加载地图瓦片（Tile）并渲染由数据库坐标数据生成的点、线、面。

**核心机制**：将经纬度坐标转换为屏幕像素坐标，管理地图的缩放、平移、图层叠加和交互事件。

**在本项目中的作用**：台风路径可视化（高德矢量底图 + Leaflet 多段折线 + CircleMarker 标注）、地震震中展示（CircleMarker 大小/颜色自适应震级）、龙卷风位置展示（OpenStreetMap 底图 + CircleMarker）。同时作为 Canvas 2D 图层的承载容器。

---

### 9. Canvas 2D（浏览器原生绘图 API）

**是什么**：HTML5 提供的基于 JavaScript 的二维像素级绘图接口。不同于 SVG（每个图形是一个独立 DOM 节点），Canvas 直接控制屏幕像素。

**核心机制**：通过 JavaScript 调用 Canvas API 在 `<canvas>` 元素上绘制图形。绘制完成后浏览器只感知一张像素图，不维护每个数据点的独立 DOM 节点，因此处理大量数据点（如数千个散点）时性能远超 DOM 方案。

**在本项目中的作用**：绘制地震区域活动趋势图（震级散点 + 水平误差气泡）、地震事件属性对比柱状图（当前事件 vs 区域均值）、台风强度双轴时序图（风速 + 气压）、龙卷风 EF 等级频次分布柱状图。相比 ECharts 等图表库，Canvas 编程难度更高但灵活性不受限制。

**Canvas 与 ECharts 的关系**：ECharts 是 Canvas 的高级封装——提供数据直接生成图表。本项目选 Canvas 自绘是为了避免引入额外第三方依赖，同时获得完全的绘图自由度。

---

## 三、后端框架层

### 10. FastAPI 0.115（后端 API 框架）

**是什么**：FastAPI 是一个基于 Python 的现代异步 Web 框架，专为构建高性能 REST API 设计。基于 ASGI 标准（而非传统 WSGI），支持异步请求处理。

**核心机制**：依赖注入（`Depends`）——认证、数据库会话等公共逻辑通过函数参数注入。Pydantic 集成——请求参数和响应数据自动进行类型校验和序列化。OpenAPI 自动生成——代码写完即自动产出交互式 API 文档（访问 `/docs`）。

**本项目中 FastAPI 与 5.0 版本 Flask 的关键差异**：

| 方面 | Flask（v5.0） | FastAPI（v6.1） |
|------|--------------|-----------------|
| 定位 | Web 应用框架 | API 开发框架 |
| 通信标准 | WSGI（同步） | ASGI（异步支持） |
| 数据校验 | 手动写代码 | Pydantic 自动 |
| API 文档 | 需额外配置 | 自动生成 Swagger |
| 性能 | 中等 | 高 |

**在本项目中的作用**：10 个路由模块、约 40 个 API 端点，全部由 FastAPI 承载。接收前端请求、校验参数、查询数据库（通过 SQLAlchemy）、调用 DeepSeek AI、返回 JSON 响应。

---

### 11. Uvicorn 0.30（ASGI 服务器）

**是什么**：Uvicorn 是一个基于 ASGI（Asynchronous Server Gateway Interface）标准的高性能 Python Web 服务器，用于运行 FastAPI 等现代异步 Web 应用。

**与本项目的关系**：FastAPI 定义接口逻辑（"这个 URL 应该做什么"），Uvicorn 负责网络层（"监听端口 5000、接收 HTTP 请求、并发处理多用户、将请求分发给 FastAPI"）。本地开发使用 `--reload` 参数实现代码修改后自动重启。

---

## 四、数据库与数据访问层

### 12. PostgreSQL 15+（关系型数据库）

**是什么**：PostgreSQL 是一个功能强大的开源对象关系型数据库管理系统，被广泛认为是科研和企业级应用的首选数据库。支持复杂 SQL 查询、高级数据类型（JSONB、ARRAY、tsvector）、事务完整性和扩展机制。

**本项目选择 PostgreSQL 而非 MySQL 的原因**：

- 空间数据分析需求——只有 PostgreSQL 有成熟的 PostGIS 空间扩展，MySQL 的空间能力远不足以支撑本项目需求
- 全文检索能力——PostgreSQL 的 tsvector + GIN 索引比 MySQL 的 FULLTEXT 索引功能强大得多
- 复杂查询性能——地震统计、邻近台风查询等聚合分析在 PostgreSQL 上执行效率更高

**在本项目中的作用**：存储全部 17 张表的所有数据——用户、文献全文、关键词、引用关系、台风路径点、地震参数、龙卷风事件、AI 分析缓存、搜索历史等。

---

### 13. PostGIS 3.x（空间数据库扩展）

**是什么**：PostGIS 是 PostgreSQL 的空间数据库扩展，为数据库增加 GIS（地理信息系统）能力。它将普通的经纬度数字升级为真正的空间对象（POINT、LINESTRING、POLYGON），并提供空间运算函数（距离计算、邻近搜索、面积计算等）。

**核心能力**：

- 空间数据类型：`geometry(Point, 4326)` / `geometry(LineString, 4326)` / `geometry(Polygon, 4326)`（EPSG:4326 为 WGS-84 全球坐标系统）
- 空间索引：GIST 索引大幅加速空间查询
- 空间函数：`ST_DWithin`（邻近搜索）、`ST_Distance`（距离计算）、`ST_Centroid`（质心）、`ST_MakePoint`（创建点）

**在本项目中的作用**：

- 台风：`typhoon_info.track_geom`（LineString 轨迹线）+ `typhoon_track.geom`（Point 观测点）
- 地震：`earthquake_info.epicenter`（Point 震中点）
- 龙卷风：`tornado_info.geom`（Point 发生地点）
- 空间密度分析：三模块各自用 `ST_DWithin` 计算邻近灾害的统计（台风 500km、地震 300km、龙卷风 100km），**距离使用 `geography` 类型确保按真实公里计算而非按经纬度度数**

**自动维护机制**：通过 PostgreSQL 触发器（`set_geom_from_lonlat()`），每次写入经纬度数据时自动生成对应的 PostGIS geometry 列，无需业务代码手动操作。

---

### 14. SQLAlchemy 2.0（对象关系映射框架）

**是什么**：SQLAlchemy 是 Python 生态中最成熟的 ORM（Object Relational Mapping，对象关系映射）框架。它将数据库中的表映射为 Python 类，使开发者可以用面向对象方式操作数据。

**本项目的特殊用法（混合模式）**：

- `models/__init__.py` 中定义了 17 张表的 ORM 模型类（表结构描述）
- 路由中的实际查询全部使用 `text()` 写原始 SQL

**为什么不全用 ORM**：本项目的空间查询（PostGIS `ST_DWithin`）、全文检索（`plainto_tsquery`）、复杂聚合统计（`EXTRACT(YEAR FROM ...)` + `SUM(CASE WHEN ...)`）若用 ORM 的查询构建器表达会极其冗长。原始 SQL 在此场景下更清晰、更好优化。

**SQLAlchemy 在此的核心价值**：数据库引擎管理（连接池、事务）、会话生命周期管理（`get_db()` 依赖注入，每个请求一个会话）、模型定义作为表结构文档参考。

---

### 15. psycopg2 2.9（PostgreSQL 数据库驱动）

**是什么**：psycopg2 是 PostgreSQL 官方推荐的 Python 数据库驱动，负责在 Python 程序和 PostgreSQL 数据库之间建立底层通信（TCP 连接、SQL 语句发送、结果集接收）。

**与本项目的关系**：SQLAlchemy 通过 psycopg2 连接 PostgreSQL。三层结构为 `FastAPI → SQLAlchemy → psycopg2 → PostgreSQL`。SQLAlchemy 负责数据库操作的抽象和代码组织，psycopg2 负责实际的网络通信。

---

### 16. geoalchemy2 0.15（PostGIS ORM 适配）

**是什么**：geoalchemy2 是 SQLAlchemy 的 PostGIS 扩展适配器，使 SQLAlchemy 的 ORM 模型能正确定义和处理 PostGIS 的 geometry/geography 空间数据类型。

**在本项目中的作用**：ORM 模型中通过 `geoalchemy2.Geometry('POINT', 4326)` 等类型声明，使数据库表结构定义层与 PostGIS 空间列兼容。

---

## 五、全文检索层

### 17. PostgreSQL tsvector + GIN 索引（全文检索引擎）

**是什么**：PostgreSQL 原生的全文检索机制。`tsvector` 是一种特殊数据类型，将文本分解为词位（lexeme）并存储为可搜索向量。`GIN`（Generalized Inverted Index，广义倒排索引）是用于加速 tsvector 搜索的索引结构。

**关键参数选择**：使用 `simple` 分词配置（而非 `english`），原因如下：

- `simple` 不做词干提取（stemming），中文单字不会因为 "不存在英文词干" 而被丢弃
- `simple` 直接按空格和非字母数字字符分词，与中文的 ILIKE 兜底策略兼容最好
- 英文搜索在 `simple` 下也不做词干提取，精确匹配更可靠

**权重设计**：标题（A 权重）> 摘要（B 权重）> 全文（C 权重），通过触发器 `documents_tsv_trigger()` 自动维护。

**搜索策略（混合模式）**：

```sql
WHERE (
  full_text_tsv @@ plainto_tsquery('simple', :kw)  -- tsvector 主搜索，走 GIN 索引
  OR full_text ILIKE '%kw%'                         -- ILIKE 中文兜底
)
ORDER BY ts_rank_cd(full_text_tsv, query) DESC      -- 相关性排序
```

**在本项目中的作用**：替代 v5.0 的全表扫描 `LIKE '%kw%'` 全文搜索，大幅提升搜索速度和结果相关性。标题/作者/分类字段因数据量小仍保留 ILIKE 方案。

---

## 六、安全认证层

### 18. PyJWT 2.10（JSON Web Token 认证）

**是什么**：PyJWT 是 Python 中 JWT（JSON Web Token）标准（RFC 7519）的实现库。JWT 是一种无状态的身份认证令牌，由三部分组成：Header（算法标识）、Payload（用户信息 + 过期时间）、Signature（签名防篡改）。

**核心机制**：

- 签发：登录成功后，服务器用 `SECRET_KEY` 对包含用户 ID 和用户名的 Payload 进行 HMAC-SHA256 签名，生成 token 返回前端
- 携带：前端将 token 存入 `localStorage`，后续每次请求通过 Axios 拦截器自动附加到 `Authorization: Bearer <token>` 头
- 验证：后端收到请求后，用同一 `SECRET_KEY` 解密并验证签名和过期时间
- 过期：24 小时后 token 自动失效，用户需重新登录

**JWT vs Session 的选择**：

| 维度 | Session | JWT |
|------|---------|-----|
| 状态存储位置 | 服务器内存/数据库 | 客户端 |
| 分布式扩展 | 需共享 Session | 天然支持 |
| 本项目适用性 | 一般 | 优秀 |

前端即客户端，后端不保存用户会话状态，这在前后端分离架构中更自然。

**v5.0 历史**：v5.0 使用的是 `python-jose`（功能相同但维护较少的库），v6.1 统一迁移至 PyJWT。

---

### 19. Werkzeug 3.0+（密码哈希）

**是什么**：Werkzeug 是 Python WSGI 工具库（Flask 的底层依赖），本项目使用其 `generate_password_hash()` 和 `check_password_hash()` 函数进行密码的不可逆哈希加密。

**核心机制**：`scrypt` 算法——将密码原文通过加密哈希函数转换为固定长度的密文。同一密码每次哈希结果不同（因为加盐 salt），但可以通过 `check_password_hash()` 验证原文是否与哈希匹配。即使数据库被泄露，攻击者也无法从哈希值反推密码原文。

**在本项目中的作用**：用户密码和安全问题答案全部使用 Werkzeug 哈希存储，数据库中永不出现明文密码。

**保留原因**：v6.1 后端从 Flask 迁到 FastAPI，但密码哈希算法保留 Werkzeug 实现，因为与 5.0 版本的数据兼容（已有用户的哈希密码可直接验证）。

---

## 七、辅助工具层

### 20. pdfplumber 0.11（PDF 文本提取）

**是什么**：Python 的 PDF 内容提取库，能够从 PDF 文件中提取文本、表格和元数据。

**在本项目中的作用**：用户上传 PDF 文献后，后端在保存文件的同时调用 pdfplumber 逐页提取全文文本，存入 `documents.full_text` 字段。随后 PostgreSQL 的 `trg_documents_tsv` 触发器自动将全文转换为 tsvector 检索向量，使该文献能被全文搜索命中。

---

### 21. Pillow 10.4（验证码图片生成）

**是什么**：Pillow 是 Python 图像处理库（PIL 的分支），提供图片创建、绘制、格式转换等功能。

**在本项目中的作用**：在 `services/__init__.py` 中生成 160×60 像素的 PNG 验证码图片——随机浅色背景、5 条干扰线、100 个噪点、4 位随机字符（大写字母 + 数字，每个字符有随机偏移和颜色）。验证码文本通过 HttpOnly Cookie 存储（JavaScript 不可读取，防自动化脚本），有效期 5 分钟。

---

### 22. DeepSeek API（大模型 AI 分析）

**是什么**：由 DeepSeek 提供的大语言模型 API。本项目调用模型为 `deepseek-chat`。

**核心机制**：HTTP POST 请求 → `/chat/completions` 端点，携带 system prompt（角色设定）和 user prompt（具体灾害参数）。参数配置为 `temperature=0.3`（低随机性，输出稳定）和 `max_tokens=350`（约 100-150 字中文）。

**在本项目中的作用**：为台风（4 种分析类型）、地震（3 种）、龙卷风（3 种）提供 AI 科学解读。首次调用结果写入对应 `*_ai_analysis` 缓存表，后续相同请求直接读缓存，不消耗 API 额度。

---

## 技术栈清单（22 项）

| 序号  | 技术             | 类别             | 版本            | 加载方式          |
| --- | -------------- | -------------- | ------------- | ------------- |
| 1   | Vue            | 前端框架           | 3.5           | npm           |
| 2   | Vite           | 前端构建           | 6.0           | npm           |
| 3   | Pinia          | 状态管理           | 2.3           | npm           |
| 4   | Vue Router     | 前端路由           | 4.5           | npm           |
| 5   | Axios          | HTTP 客户端       | 1.7           | npm           |
| 6   | Tailwind CSS   | 样式框架           | CDN           | CDN           |
| 7   | Font Awesome   | 图标库            | 4.7           | CDN           |
| 8   | Leaflet        | Web 地图         | 1.9           | npm           |
| 9   | Canvas 2D      | 浏览器绘图          | 内置            | 浏览器原生         |
| 10  | FastAPI        | 后端框架           | 0.115         | pip           |
| 11  | Uvicorn        | ASGI 服务器       | 0.30          | pip           |
| 12  | PostgreSQL     | 关系数据库          | 15+           | 系统安装          |
| 13  | PostGIS        | 空间扩展           | 3.x           | PostgreSQL 扩展 |
| 14  | SQLAlchemy     | ORM 框架         | 2.0           | pip           |
| 15  | psycopg2       | Python-PG 驱动   | 2.9           | pip           |
| 16  | geoalchemy2    | PostGIS ORM 适配 | 0.15          | pip           |
| 17  | tsvector + GIN | 全文检索           | PG 内置         | PostgreSQL 内置 |
| 18  | PyJWT          | JWT 认证         | 2.10          | pip           |
| 19  | Werkzeug       | 密码哈希           | 3.0+          | pip           |
| 20  | pdfplumber     | PDF 提取         | 0.11          | pip           |
| 21  | Pillow         | 验证码生成          | 10.4          | pip           |
| 22  | DeepSeek API   | AI 大模型         | deepseek-chat | HTTP API      |

---

## 加载方式说明

- **npm**：通过 Node.js 包管理器下载到 `frontend/node_modules/`，由 Vite 在构建时打包进最终产物
- **pip**：通过 Python 包管理器安装到系统 Python 环境，由 Uvicorn 在运行时加载
- **CDN**：不下载到本地，浏览器打开网页时从 CDN 服务器的网络地址在线加载。优点是无需安装、减小项目体积，缺点是需要网络连接
- **浏览器原生**：Canvas 2D 是 HTML5 标准的一部分，所有现代浏览器内置支持，无需任何安装或加载
- **系统安装/PostgreSQL 扩展**：需要在操作系统或数据库层面预先安装，然后在数据库中以 SQL 命令激活
- **HTTP API**：通过网络 HTTP 协议调用外部服务

---

> **文档版本**：v1.0 | **对应项目版本**：v6.1.0  

© 2026 南京农业大学 · 信息与计算科学专业 · 丁俊杰
