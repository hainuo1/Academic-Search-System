# MeteoScholar v6.1 — 项目文件逐一说明

> **用途**：配合《技术栈说明》文档，逐一解释本项目中每一个文件的功能、所属层级和关联技术。
> **项目**：MeteoScholar v6.1（前后端分离科研平台）
> **作者**：丁俊杰（南京农业大学，hainuo@stu.njau.edu.cn）

---

## 阅读指南

本文档按目录树顺序逐一说明每个文件。每个文件条目包含三行信息：

- **路径**：文件在项目中的位置
- **类型**：配置文件 / 路由模块 / 数据模型 / 页面组件 / 工具函数 等
- **说明**：这个文件做什么、属于技术栈的哪一层、关联哪些技术

阅读时建议先看"文件总览"表格建立整体印象，再按需深入单个文件的详细说明。如果只想了解某个目录，直接跳到对应的二级标题。

---

## 文件总览

| 序号 | 文件路径 | 类型 | 一句话定位 |
|---|---|---|---|
| 1 | `.gitattributes` | Git配置 | CSV 大文件由 Git LFS 管理 |
| 2 | `手写文档.md` | 文档 | 项目技术栈完整说明 |
| 3 | `backend/.env` / `.env.example` | 配置 | 环境变量（数据库、密钥、API Key） |
| 4 | `backend/requirements.txt` | 配置 | Python 后端依赖清单 |
| 5 | `backend/app/main.py` | 后端入口 | FastAPI 应用创建、路由注册、CORS |
| 6 | `backend/app/core/config.py` | 配置 | 环境变量加载与校验（pydantic-settings） |
| 7 | `backend/app/core/database.py` | 数据层 | SQLAlchemy 引擎与会话工厂 |
| 8 | `backend/app/core/security.py` | 安全 | JWT 生成、验证、依赖注入函数 |
| 9 | `backend/app/models/__init__.py` | 数据模型 | 18 张表的 ORM 模型定义 |
| 10~20 | `backend/app/routers/*.py` (11个) | 路由模块 | API 端点实现（业务逻辑） |
| 21 | `backend/app/schemas/__init__.py` | 数据校验 | Pydantic 请求/响应模型 |
| 22 | `backend/app/services/__init__.py` | 服务层 | 验证码生成 + DeepSeek AI 调用 |
| 23 | `frontend/index.html` | 入口页面 | SPA 单页面入口，加载 CDN 资源 |
| 24 | `frontend/package.json` | 配置 | 前端 npm 依赖清单 |
| 25 | `frontend/vite.config.ts` | 构建配置 | Vite 构建工具配置 |
| 26 | `frontend/tsconfig.json` | TS配置 | TypeScript 编译选项 |
| 27 | `frontend/src/main.js` | 前端入口 | Vue 应用初始化 |
| 28 | `frontend/src/App.vue` | 根组件 | 全局导航栏 + 页脚 + 路由视图 |
| 29 | `frontend/src/api/index.js` | 通信层 | Axios 实例 + 拦截器 |
| 30 | `frontend/src/router/index.js` | 路由 | 24 条前端路由 + 导航守卫 |
| 31 | `frontend/src/stores/auth.js` | 状态管理 | Pinia 用户认证状态 |
| 32 | `frontend/src/stores/flash.ts` | 状态管理 | Pinia 全局消息提示 |
| 33 | `frontend/src/stores/flash.js` | 状态管理 | ⚠️ flash.ts 的旧版副本，建议删除 |
| 34~57 | `frontend/src/views/*.vue` (24个) | 页面组件 | 各功能页面实现 |

---

## 一、项目根目录

### 1. `.gitattributes`

- **路径**：`6.1版本/.gitattributes`
- **类型**：Git 配置文件
- **关联技术**：Git LFS

```
*.csv filter=lfs diff=lfs merge=lfs -text
```

指定所有 `.csv` 文件由 Git LFS（Large File Storage）管理。数据集目录下的 CSV 文件每份可达数百 MB，Git LFS 将其存储为大文件指针而非直接将二进制数据纳入版本库，避免仓库膨胀。与代码文件区分管理。

---

### 2. `手写文档.md`

- **路径**：`6.1版本/手写文档.md`
- **类型**：项目文档
- **关联技术**：全部 23 项核心技术

项目的技术栈完整说明文档，面向零基础读者按七层架构讲解每一项技术选型。包含整体架构图、每项技术的"是什么—为什么—在本项目中的作用"三层讲解、附录（六种依赖加载方式）、完整技术清单表。

---

## 二、数据集目录

### `数据集/`

- **路径**：`6.1版本/数据集/`
- **类型**：原始数据
- **关联技术**：PostgreSQL（目标存储）

存放三大灾害模块的原始 CSV 数据文件，由独立的导入脚本写入 PostgreSQL 数据库。导入完成后不参与系统运行时逻辑。

| 子目录/文件 | 内容 | 对应数据表 |
|---|---|---|
| `热带气旋/ibtracs.ALL.list.v04r01.csv` | IBTrACS 全球热带气旋历史路径数据 | `typhoon_info`, `typhoon_track` |
| `地震/1959-1966.csv` ~ `2016-2026.csv` | USGS 全球地震目录（按年代分卷） | `earthquake_info` |
| `龙卷风/1950-2024_actual_tornadoes.csv` | 中国历史龙卷风记录 | `tornado_info` |

⚠️ 这些 CSV 文件不参与项目检索范围。它们通过独立的导入脚本处理后存入 PostgreSQL，前端只从数据库读取。

---

## 三、后端 (`backend/`)

后端是 Python FastAPI 应用，采用分层架构：`main.py`（入口）→ `routers/`（路由）→ `services/`（服务）→ `models/`（数据模型）→ `core/`（基础设施）。

---

### 3. `backend/.env` / `backend/.env.example`

- **路径**：`6.1版本/backend/.env`、`6.1版本/backend/.env.example`
- **类型**：环境变量配置文件
- **关联技术**：pydantic-settings 2.5

`.env` 保存实际运行配置（数据库密码、SECRET_KEY、DeepSeek API Key 等敏感信息），不纳入 Git 版本控制。`.env.example` 是模板文件，包含所有需配置的键名和占位值，新开发者复制为 `.env` 后填入真实值即可。

```
关键配置项：
APP_NAME       → 应用名称
SECRET_KEY     → JWT 签名密钥（生产环境须更换）
DB_HOST/PORT   → PostgreSQL 连接信息
DB_NAME        → 数据库名（AcademicSearchDB）
DEEPSEEK_API_KEY → AI 分析功能密钥
CORS_ORIGINS   → 允许跨域的前端地址
```

由 `core/config.py` 中的 `Settings` 类通过 `model_config = {"env_file": ".env"}` 自动加载。

---

### 4. `backend/requirements.txt`

- **路径**：`6.1版本/backend/requirements.txt`
- **类型**：Python 依赖声明文件
- **关联技术**：pip

列出后端全部 14 个 Python 包及其精确版本。`pip install -r requirements.txt` 一键安装全部依赖。

```
fastapi==0.115.0          ← 后端 Web 框架
uvicorn[standard]==0.30.6 ← ASGI 服务器
sqlalchemy==2.0.35        ← ORM 框架
psycopg2-binary==2.9.9    ← PG 驱动
pydantic==2.9.2           ← 数据校验框架
pydantic-settings==2.5.2  ← 环境变量管理
PyJWT==2.10.1             ← JWT 认证
cryptography==43.0.3      ← 加密底层（PyJWT 依赖）
python-multipart==0.0.12  ← 文件上传解析
werkzeug>=3.0.4           ← 密码哈希
pdfplumber==0.11.4        ← PDF 文本提取
requests==2.32.3          ← DeepSeek API HTTP 调用
Pillow==10.4.0            ← 验证码图片生成
geoalchemy2==0.15.2       ← PostGIS ORM 适配
```

---

### 5. `backend/app/main.py`

- **路径**：`6.1版本/backend/app/main.py`
- **类型**：后端应用入口
- **关联技术**：FastAPI 0.115、Uvicorn 0.30

FastAPI 应用的主入口文件。启动命令 `uvicorn app.main:app --reload --host 0.0.0.0 --port 5000` 中的 `app.main:app` 即指向此文件中的 `app = FastAPI()` 实例。

**核心职责：**
- 创建 FastAPI 应用实例，设置标题和版本
- 配置 CORS 中间件（允许 `localhost:5173` 跨域访问，支持 `withCredentials`）
- 确保上传目录存在
- 注册全部 11 个路由模块
- 注册全局异常处理（404、500）
- 定义根路径 `/` 健康检查接口

**启动方式：** `uvicorn app.main:app --reload --port 5000`

---

### 6. `backend/app/core/__init__.py`

- **路径**：`6.1版本/backend/app/core/__init__.py`
- **类型**：Python 包初始化文件
- **说明**：使 `core` 成为 Python 包。内容为空。

---

### 7. `backend/app/core/config.py`

- **路径**：`6.1版本/backend/app/core/config.py`
- **类型**：配置管理
- **关联技术**：pydantic-settings 2.5

定义 `Settings` 类，继承 `pydantic_settings.BaseSettings`。所有配置项通过类型注解声明，`BaseSettings` 自动按优先级读取：环境变量 > `.env` 文件 > 默认值。

**关键配置组：**

| 配置组 | 字段 | 说明 |
|---|---|---|
| 应用 | APP_NAME, VERSION, DEBUG, SECRET_KEY | 应用标识和调试模式 |
| 数据库 | DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME | PostgreSQL 连接参数，通过 `DATABASE_URL` 属性拼装为连接字符串 |
| JWT | JWT_ALGORITHM, JWT_EXPIRY_HOURS | Token 签名算法（HS256）和有效期（24h） |
| 上传 | UPLOAD_DIR, MAX_UPLOAD_SIZE | 上传目录（相对路径）和大小限制（50MB） |
| AI | DEEPSEEK_API_KEY, DEEPSEEK_API_BASE, DEEPSEEK_MODEL | DeepSeek API 配置 |
| CORS | CORS_ORIGINS | 允许跨域的前端地址列表 |

全局单例 `settings = Settings()` 被其他模块通过 `from app.core.config import settings` 引用。

---

### 8. `backend/app/core/database.py`

- **路径**：`6.1版本/backend/app/core/database.py`
- **类型**：数据库基础设施
- **关联技术**：SQLAlchemy 2.0、psycopg2 2.9

创建 SQLAlchemy 数据库引擎和会话工厂。

**核心要素：**

| 要素 | 变量名 | 说明 |
|---|---|---|
| 数据库引擎 | `engine` | 通过 `create_engine(settings.DATABASE_URL)` 创建，配置连接池（pool_size=10, max_overflow=20, pool_pre_ping=True） |
| 会话工厂 | `SessionLocal` | 每次调用产生一个新的事务会话 |
| 基类 | `Base` | `declarative_base()`，所有 ORM 模型继承自此 |
| 依赖注入 | `get_db()` | FastAPI `Depends` 函数，每个请求创建一个会话，请求结束自动关闭 |

`DATABASE_URL` 格式为 `postgresql+psycopg2://`，说明 SQLAlchemy 通过 psycopg2 驱动连接 PostgreSQL。

---

### 9. `backend/app/core/security.py`

- **路径**：`6.1版本/backend/app/core/security.py`
- **类型**：安全认证模块
- **关联技术**：PyJWT 2.10、FastAPI Depends

JWT 令牌的生成、验证和 FastAPI 依赖注入。

| 函数 | 作用 | 被谁调用 |
|---|---|---|
| `create_access_token(user_id, username)` | 生成 JWT Token：payload 含 user_id、username、exp（过期时间）、iat（签发时间），用 SECRET_KEY 和 HS256 签名 | `routers/auth.py` 登录接口 |
| `verify_token(token)` | 解码并验证 Token，过期或无效返回 None | `get_current_user()` 内部 |
| `get_current_user(credentials)` | FastAPI 依赖函数：从 `Authorization: Bearer <token>` 请求头提取 Token → 调用 verify_token → 通过则返回 payload，失败则 401 | 所有需要登录的接口（通过 `Depends(get_current_user)`） |

Token 过期时间为 24 小时（由 `config.py` 的 `JWT_EXPIRY_HOURS` 控制）。

---

### 10. `backend/app/models/__init__.py`

- **路径**：`6.1版本/backend/app/models/__init__.py`
- **类型**：ORM 数据模型定义
- **关联技术**：SQLAlchemy 2.0、PostgreSQL 15+、PostGIS 3.x、geoalchemy2 0.15

定义全部 18 张数据库表的 ORM 模型。每张表对应一个 Python 类，继承自 `Base = declarative_base()`。

**18 张表按业务模块分组：**

**核心检索系统（11张）：**

| 类名 | 表名 | 说明 |
|---|---|---|
| `User` | `users` | 用户账号：用户名、密码哈希、邮箱、安全问题、登录锁定 |
| `Document` | `documents` | 文献：标题、作者、摘要、发表日期、分类、文件路径、全文、浏览/下载计数 |
| `LiteratureCategory` | `literature_categories` | 文献分类标签：预定义 29 个学科分类（如"人工智能"、"气象科学"），上传时从列表选择 |
| `Keyword` | `keywords` | 关键词字典：去重后的关键词集合 |
| `DocumentKeyword` | `document_keyword` | 文献-关键词多对多关联表，含 TF-IDF 权重 |
| `Citation` | `citation` | 引用关系：source_document_id 引用 target_document_id，带自引用检查约束 |
| `Favorite` | `favorites` | 用户收藏：user_id + document_id 唯一约束 |
| `SearchHistory` | `search_history` | 搜索历史：自动保留最近 100 条 |
| `BrowseHistory` | `browse_history` | 浏览历史：每次查看文献详情时记录 |
| `UserSearchCount` | `user_search_count` | 用户搜索次数统计 |
| `KeywordSearchCount` | `keyword_search_count` | 关键词搜索热度排行 |

**台风模块（3张）：**

| 类名 | 表名 | 说明 |
|---|---|---|
| `TyphoonInfo` | `typhoon_info` | 台风基本信息：名称、季节、海域、最大风速、最低气压、起止时间 |
| `TyphoonTrack` | `typhoon_track` | 台风路径点：经纬度、风速、气压、风暴等级、七级/十级风圈半径 |
| `TyphoonAIAnalysis` | `typhoon_ai_analysis` | AI 分析缓存：4 种分析类型（trend/compare/impact/travel） |

**地震模块（2张）：**

| 类名 | 表名 | 说明 |
|---|---|---|
| `EarthquakeInfo` | `earthquake_info` | 地震事件：震级、深度、位置、海啸、预警、误差参数等 |
| `EarthquakeAIAnalysis` | `earthquake_ai_analysis` | AI 分析缓存：3 种分析类型（trend/risk/impact） |

**龙卷风模块（2张）：**

| 类名 | 表名 | 说明 |
|---|---|---|
| `TornadoInfo` | `tornado_info` | 龙卷风事件：EF 等级、伤亡、经济损失、位置、省份 |
| `TornadoAIAnalysis` | `tornado_ai_analysis` | AI 分析缓存：3 种分析类型（trend/risk/climate） |

⚠️ 本项目采用混合模式：ORM 模型用于定义表结构和管理连接，路由中的实际查询全部写原生 SQL（`text()`），因为空间查询和全文检索用 ORM 表达过于冗长。

---

### 11~21. `backend/app/routers/` — 路由模块（11个文件）

路由层是系统的业务逻辑核心。每个路由文件对应一个功能域，内部使用原生 SQL 查询数据库。

---

#### 11. `backend/app/routers/__init__.py`

- **类型**：Python 包初始化
- **说明**：空文件，使 `routers` 成为 Python 包。

---

#### 12. `backend/app/routers/auth.py`

- **类型**：认证路由
- **关联技术**：FastAPI、Werkzeug、PyJWT、Pillow
- **前缀**：`/api`
- **端点**：6 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/login` | POST | 用户登录：验证密码 → 检查锁定状态 → 生成 JWT → 返回 Token |
| `/api/register` | POST | 用户注册：校验用户名/密码/邮箱格式 → 哈希密码 → 写入 users 表 |
| `/api/forgot_password` | POST | 找回密码（两步）：step1 验证码验证 → step2 安全问题验证 → 重置密码 |
| `/api/logout` | POST | 退出登录（前端清除 Token，后端返回确认） |
| `/api/captcha` | GET | 生成验证码图片（PNG），通过 HttpOnly Cookie 传递验证码文本 |
| `/api/user/me` | GET | 获取当前登录用户信息（用户名 + 脱敏邮箱） |

**安全机制：**
- 登录失败 5 次 → 锁定 30 分钟
- 密码使用 Werkzeug `generate_password_hash()` 存储
- 安全问题答案也经过哈希存储
- 验证码有效期 5 分钟，通过 HttpOnly Cookie 防止 XSS 读取

---

#### 13. `backend/app/routers/search.py`

- **类型**：检索路由
- **关联技术**：PostgreSQL tsvector + GIN、SQLAlchemy
- **前缀**：`/api`
- **端点**：2 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/search` | GET | 文献多条件检索：支持标题/作者/分类/关键词/全文等多种搜索类型，分页返回 |
| `/api/history` | GET | 搜索历史 + 浏览历史（双列表分页） |

**检索核心机制（`_build_rank_clause` 函数）：**
- `fulltext` 模式：tsvector 主搜索 + ILIKE 中文兜底，按相关性排序（`ts_rank_cd`）
- `keyword` 模式：通过 `document_keyword` 关联表匹配
- `title/author/category` 模式：ILIKE 模糊匹配
- 全文过滤（`fulltext_filter`）可与主搜索关键词独立组合
- 每次搜索自动记录到 `search_history`、`user_search_count`、`keyword_search_count` 表

---

#### 14. `backend/app/routers/document.py`

- **类型**：文献操作路由
- **关联技术**：FastAPI、pdfplumber、PostgreSQL
- **前缀**：`/api`
- **端点**：8 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/document/{did}` | GET | 文献详情：含引用关系（被引+引用）、关键词、是否已收藏 |
| `/api/upload` | POST | 文献上传：接收 PDF + 表单字段 → pdfplumber 提取全文 → 存入数据库 |
| `/api/download/{did}` | GET | 文献下载：记录下载次数，返回 PDF 文件流 |
| `/api/download/{did}/check` | GET | 检查 PDF 文件是否存在（下载前预检） |
| `/api/my_documents` | GET | 当前用户上传的文献列表 |
| `/api/document/{did}` | DELETE | 删除文献：级联删除引用、收藏、浏览记录、关键词关联和物理文件 |
| `/api/document/{did}/edit-data` | GET | 获取文献编辑数据（仅上传者本人可访问） |
| `/api/document/{did}` | PUT | 更新文献元数据（仅上传者本人） |

**文件处理流程：** 上传 PDF → `uuid.uuid4().pdf` 重命名 → 存入 `backend/uploads/` → pdfplumber 提取全文 → 写入 `documents.full_text` → 数据库触发器自动生成 tsvector

---

#### 15. `backend/app/routers/favorites.py`

- **类型**：收藏夹路由
- **关联技术**：FastAPI、PostgreSQL
- **前缀**：`/api`
- **端点**：2 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/favorites` | GET | 收藏列表（分页） |
| `/api/toggle_favorite` | POST | 切换收藏状态：已收藏→取消，未收藏→添加 |

---

#### 16. `backend/app/routers/citation.py`

- **类型**：引用关系路由
- **关联技术**：FastAPI、PostgreSQL
- **前缀**：`/api`
- **端点**：3 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/citation/{did}` | GET | 获取文献引用关系（仅上传者可查看编辑界面） |
| `/api/search_citation` | GET | 搜索可引用的文献（按标题 ILIKE） |
| `/api/save_citation/{did}` | POST | 保存引用关系：先删后插，批量写入 target_doc_ids |

---

#### 17. `backend/app/routers/profile.py`

- **类型**：个人中心路由
- **关联技术**：FastAPI、Werkzeug
- **前缀**：`/api/profile`
- **端点**：4 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/profile` | GET | 获取个人资料（用户名 + 脱敏邮箱 + 安全问题） |
| `/api/profile/email` | PUT | 修改邮箱（需验证当前密码） |
| `/api/profile/password` | PUT | 修改密码（需验证当前密码，新密码 Werkzeug 哈希后存储） |
| `/api/profile/security` | PUT | 修改安全问题（需验证当前密码，答案哈希存储） |

---

#### 18. `backend/app/routers/stats.py`

- **类型**：统计路由
- **关联技术**：FastAPI、PostgreSQL
- **前缀**：`/api`
- **端点**：1 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/stats` | GET | 六维统计排行：关键词热度 TOP10、用户搜索量 TOP10、文献被引量 TOP10、浏览量 TOP10、下载量 TOP10、收藏量 TOP10 |

---

#### 19. `backend/app/routers/typhoon.py`

- **类型**：台风分析路由
- **关联技术**：FastAPI、PostgreSQL、PostGIS、DeepSeek API
- **前缀**：`/api/typhoon`
- **端点**：6 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/typhoon/list` | GET | 台风列表：按年份/关键词/最低风速筛选，分页，附带可选年份列表 |
| `/api/typhoon/{typhoon_id}` | GET | 台风详情：基本参数 + PostGIS 500km 空间密度分析 |
| `/api/typhoon/{typhoon_id}/track` | GET | 台风路径点：全部观测点经纬度、风速、气压、风圈半径 |
| `/api/typhoon/stats/yearly` | GET | 年度统计：每年台风数量、平均/最大风速、平均/最低气压 |
| `/api/typhoon/ai_analysis` | POST | AI 分析：4 种类型（trend/compare/impact/travel），先查缓存后调 DeepSeek |
| `/api/typhoon/ai_cache` | DELETE | 清理 AI 缓存 |

**PostGIS 空间密度分析（typhoon_detail 接口）：** 以当前台风轨迹几何中心为基准，查询 500km 内其他台风数量、平均风速、最高风速等，使用 `geography` 类型确保真实公里距离。

---

#### 20. `backend/app/routers/earthquake.py`

- **类型**：地震分析路由
- **关联技术**：FastAPI、PostgreSQL、PostGIS、DeepSeek API
- **前缀**：`/api/earthquake`
- **端点**：5 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/earthquake/list` | GET | 地震列表：按年份/最低震级/地点关键词筛选 |
| `/api/earthquake/{event_id}` | GET | 地震详情：完整参数 + 邻近地震序列（±3°范围，时间轴前后排列）+ PostGIS 300km 空间密度分析 |
| `/api/earthquake/stats/yearly` | GET | 年度统计：每年地震数、平均/最大震级、强震(≥6级)/大震(≥7级)次数 |
| `/api/earthquake/ai_analysis` | POST | AI 分析：3 种类型（trend/risk/impact） |
| `/api/earthquake/ai_cache` | DELETE | 清理 AI 缓存（可按类型指定或全部清除） |

---

#### 21. `backend/app/routers/tornado.py`

- **类型**：龙卷风分析路由
- **关联技术**：FastAPI、PostgreSQL、PostGIS、DeepSeek API
- **前缀**：`/api/tornado`
- **端点**：3 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/tornado/list` | GET | 龙卷风列表：按年份/地点/最低 EF 等级筛选，附带可选年份和省份列表 |
| `/api/tornado/{event_id}` | GET | 龙卷风详情：完整参数 + 同省份历史事件 + PostGIS 100km 空间密度分析 |
| `/api/tornado/ai_analysis` | POST | AI 分析：3 种类型（trend/risk/climate） |

**`_parse_damage` 辅助函数：** 将原始 damage 字段（如 `length:5.2mi width:200yd loss:1500000`）解析为中文可读格式（如"路径长度 5.2 mi（约8.4 km）· 宽度 200 yd（约0.18 km）· 经济损失 $1.5M"）。

---

#### 22. `backend/app/routers/category.py`

- **类型**：分类标签路由
- **关联技术**：FastAPI、PostgreSQL
- **前缀**：`/api`
- **端点**：5 个

| 端点 | 方法 | 功能 |
|---|---|---|
| `/api/categories` | GET | 获取所有分类标签（按 sort_order 排序） |
| `/api/categories` | POST | 添加新分类标签 |
| `/api/categories/{cat_id}` | DELETE | 删除分类标签 |
| `/api/categories/seed` | POST | 导入 29 个预设分类（人工智能→医学） |
| `/api/categories/search` | GET | 检索分类标签（供上传页选择器使用） |

**预设 29 个分类**涵盖计算机科学（AI/ML/NLP/CV 等）、地球科学（气象/海洋/地震/地质/GIS）、基础科学（数学/物理/化学/生物/医学）。`seed` 接口通过 `INSERT ... ON CONFLICT DO NOTHING` 逻辑避免重复导入。

---

### 23. `backend/app/schemas/__init__.py`

- **路径**：`6.1版本/backend/app/schemas/__init__.py`
- **类型**：Pydantic 数据校验模型
- **关联技术**：Pydantic 2.9

定义 FastAPI 路由中使用的请求体和响应体模型。

**请求模型（Request）：**

| 类名 | 用途 | 关联路由 |
|---|---|---|
| `LoginRequest` | 登录（username + password） | auth.py |
| `RegisterRequest` | 注册（含密码格式校验 validator） | auth.py |
| `ForgotPasswordStep1` | 找回密码第一步（验证码） | auth.py |
| `ForgotPasswordStep2` | 找回密码第二步（安全问题 + 新密码） | auth.py |
| `ChangeEmailRequest` | 修改邮箱 | profile.py |
| `ChangePasswordRequest` | 修改密码 | profile.py |
| `ChangeSecurityRequest` | 修改安全问题 | profile.py |
| `TyphoonAIRequest` | 台风 AI 分析请求 | typhoon.py |
| `EarthquakeAIRequest` | 地震 AI 分析请求 | earthquake.py |
| `TornadoAIRequest` | 龙卷风 AI 分析请求 | tornado.py |

**响应模型（Response）：**

| 类名 | 用途 |
|---|---|
| `DocumentOut` | 文献基本信息输出 |
| `DocumentDetail` | 文献详情输出（含关键词、引用、收藏状态） |
| `DocumentListOut` | 文献列表项输出 |
| `ApiResponse` | 通用响应包装（code + message + data） |

`RegisterRequest` 中使用了 Pydantic `@field_validator` 装饰器在数据进入路由前自动校验用户名和密码格式，路由函数中无需再写重复的校验代码。

---

### 24. `backend/app/services/__init__.py`

- **路径**：`6.1版本/backend/app/services/__init__.py`
- **类型**：服务层
- **关联技术**：Pillow 10.4、requests 2.32、DeepSeek API

两大核心服务函数：

**`generate_captcha()` → `(BytesIO, str)`**
- 创建 160×60 像素的 PNG 图片
- 随机浅色背景 + 5 条干扰线 + 100 个噪点
- 4 位随机大写字母+数字（从 26 字母+10 数字中选取）
- 每字符随机颜色、位置偏移 (±5px)
- 字体：优先 `arial.ttf` → 降级 `DejaVuSans-Bold.ttf` → 降级 `ImageFont.load_default()`
- 返回图片流和验证码文本，由路由写入 HttpOnly Cookie

**`call_deepseek(system_prompt, user_prompt)` → `str | None`**
- 向 DeepSeek API 发送 chat/completions 请求
- 使用 `requests.post()` 同步调用
- 参数：`temperature=0.3`（低随机性，适合科学分析），`max_tokens=350`（约 100-150 中文字）
- 超时 60 秒
- 失败（无 API Key 或网络异常）返回 None，由调用方返回 500 错误
- 使用 `settings.DEEPSEEK_API_KEY` 和 `settings.DEEPSEEK_API_BASE` 配置

---

### 25. `backend/app/uploads/`

- **路径**：`6.1版本/backend/app/uploads/`
- **类型**：文件存储目录
- **说明**：存放用户上传的 PDF 文献文件（UUID 重命名）。由 `main.py` 启动时自动创建，`.env` 中的 `UPLOAD_DIR` 配置路径。文件内容在 `.gitignore` 中排除。

---

## 四、前端 (`frontend/`)

前端是 Vue 3 SPA 应用，通过 Vite 构建。源代码在 `src/` 下按功能分层：`api/`（通信层）→ `stores/`（状态层）→ `router/`（路由层）→ `views/`（视图层）。

---

### 26. `frontend/index.html`

- **路径**：`6.1版本/frontend/index.html`
- **类型**：SPA 入口 HTML
- **关联技术**：Vite、Tailwind CSS（CDN）、Font Awesome（CDN）

整个前端应用的唯一 HTML 页面。Vue SPA 模式下所有路由都加载这个文件，Vue Router 在 `<div id="app">` 内动态替换组件。

**CDN 资源加载：**
- Tailwind CSS（`<script src="https://cdn.tailwindcss.com">`）
- Font Awesome 4.7（`<link>` 标签）

---

### 27. `frontend/package.json`

- **路径**：`6.1版本/frontend/package.json`
- **类型**：npm 依赖声明
- **关联技术**：npm、Vite、Vue、Pinia、Vue Router、Axios、Leaflet、ECharts

| 依赖 | 版本 | 分类 | 用途 |
|---|---|---|---|
| vue | ^3.5.0 | 运行时 | 前端核心框架 |
| vue-router | ^4.5.0 | 运行时 | 前端路由 |
| pinia | ^2.3.0 | 运行时 | 状态管理 |
| axios | ^1.7.0 | 运行时 | HTTP 通信 |
| leaflet | ^1.9.4 | 运行时 | 地图引擎 |
| echarts | ^6.1.0 | 运行时 | 可视化图表库 |
| @vitejs/plugin-vue | ^5.2.0 | 开发 | Vue 单文件组件编译插件 |
| vite | ^6.0.0 | 开发 | 构建工具 |

脚本：`dev`（启动开发服务器）、`build`（生产打包）、`preview`（预览打包结果）。

---

### 28. `frontend/vite.config.ts`

- **路径**：`6.1版本/frontend/vite.config.ts`
- **类型**：Vite 构建配置
- **关联技术**：Vite 6.0

```ts
export default defineConfig({
  plugins: [vue()],
  server: { port: 5173 }
})
```

仅配置了两项：Vue 单文件组件编译插件、开发服务器端口 5173。其他功能（热更新、ESBuild 编译等）使用 Vite 默认行为。

---

### 29. `frontend/tsconfig.json`

- **路径**：`6.1版本/frontend/tsconfig.json`
- **类型**：TypeScript 编译配置
- **关联技术**：TypeScript

配置 TypeScript 编译为 ES2020 目标，启用严格模式，设置 `@/*` 路径别名指向 `src/*`，包含所有 `.ts`、`.d.ts`、`.vue` 文件。

---

### 30. `frontend/tsconfig.node.json`

- **路径**：`6.1版本/frontend/tsconfig.node.json`
- **类型**：TypeScript 配置（Node 环境）
- **说明**：专用于 `vite.config.ts` 等 Node 端文件的 TypeScript 配置，与浏览器端配置分离。

---

### 31. `frontend/auto-imports.d.ts` / `frontend/components.d.ts`

- **路径**：`6.1版本/frontend/auto-imports.d.ts`、`6.1版本/frontend/components.d.ts`
- **类型**：自动生成的类型声明
- **说明**：由 Vite 插件自动生成，声明全局可用的 Vue API（如 `ref`、`computed`）和组件的 TypeScript 类型，无需手动 import。对项目功能无直接影响，但支撑 IDE 的类型提示。

---

### 32. `frontend/src/main.js`

- **路径**：`6.1版本/frontend/src/main.js`
- **类型**：前端应用入口
- **关联技术**：Vue 3.5、Pinia 2.3、Vue Router 4.5

```js
createApp(App).use(createPinia()).use(router).mount('#app')
```

一行代码完成前端应用初始化：创建 Vue 应用实例 → 安装 Pinia 状态管理 → 安装 Vue Router → 挂载到 `index.html` 的 `#app` 元素上。

---

### 33. `frontend/src/App.vue`

- **路径**：`6.1版本/frontend/src/App.vue`
- **类型**：Vue 根组件
- **关联技术**：Vue 3.5、Tailwind CSS、Font Awesome、Pinia

整个 SPA 的布局骨架。包含三部分：

- **导航栏**（`v-if="auth.isLoggedIn"`）：仅在登录后显示，含首页链接、个人中心链接、退出登录按钮。使用 Tailwind 毛玻璃效果（`bg-white/80 backdrop-blur-xl`）
- **消息提示区域**：由 Pinia flash store 驱动，四种类型（success/warning/error/info）对应不同颜色
- **主内容区**：`<router-view />` 动态渲染当前路由对应的页面组件
- **页脚**（登录后显示）：版权信息和作者联系方式

**响应式逻辑**：`auth.isLoggedIn` 控制导航栏和页脚的显示/隐藏，`flash.message` 控制全局消息提示的出现和自动消失。

---

### 34. `frontend/src/api/index.js`

- **路径**：`6.1版本/frontend/src/api/index.js`
- **类型**：HTTP 通信层
- **关联技术**：Axios 1.7

创建 Axios 实例并配置两个拦截器：

**请求拦截器：**
- 每次请求自动从 `localStorage` 读取 JWT Token
- 添加到请求头 `Authorization: Bearer <token>`
- 如果是 FormData 请求，删除 Content-Type 头（让浏览器自动设置 multipart boundary）

**响应拦截器：**
- 拦截 401 错误（非 blob 请求）
- 清除所有 localStorage
- 非登录页/欢迎页时自动跳转到 `/welcome`

**实例配置：** `baseURL: 'http://localhost:5000'`（指向 FastAPI 后端）、`timeout: 15000`（15 秒超时）、`withCredentials: true`（携带 Cookie，用于验证码）。

---

### 35. `frontend/src/router/index.js`

- **路径**：`6.1版本/frontend/src/router/index.js`
- **类型**：前端路由配置
- **关联技术**：Vue Router 4.5

定义 24 条前端路由，所有页面组件均使用懒加载（`() => import(...)`）。

**路由分类：**

| 类别 | 路由 | 组件 |
|---|---|---|
| 公开页面 | /welcome, /login, /register, /forgot_password | WelcomeView, LoginView, RegisterView, ForgotPasswordView |
| 检索系统 | /dashboard, /search, /document/:id, /upload | DashboardView, SearchView, DocumentView, UploadView |
| 文献管理 | /my_documents, /edit_document/:id, /favorites, /history | MyDocumentsView, EditDocumentView, FavoritesView, HistoryView |
| 分类引用 | /select_category, /citation/:id | CategorySelectView, CitationView |
| 个人中心 | /profile, /stats | ProfileView, StatsView |
| 台风模块 | /typhoon, /typhoon/:id | TyphoonView, TyphoonDetail |
| 地震模块 | /earthquake, /earthquake/:id | EarthquakeView, EarthquakeDetail |
| 龙卷风模块 | /tornado, /tornado/:id | TornadoView, TornadoDetail |
| 错误页面 | /:pathMatch(.*)* | ErrorView |

**导航守卫（`beforeEach`）：**
```js
const pub = ['/welcome', '/login', '/register', '/forgot_password']
const t = localStorage.getItem('token')
if (!t && !pub.includes(to.path)) next('/welcome')     // 未登录 → 强制跳转欢迎页
else if (t && pub.includes(to.path)) next('/dashboard') // 已登录 → 跳过公开页
else next()                                              // 正常放行
```

---

### 36. `frontend/src/stores/auth.js`

- **路径**：`6.1版本/frontend/src/stores/auth.js`
- **类型**：Pinia 状态存储
- **关联技术**：Pinia 2.3

管理用户认证状态。存储三个响应式变量：`token`、`username`、`userId`，以及计算属性 `isLoggedIn`（检查 token 是否非空）。

所有变量同时写入 `localStorage`，确保持久化（刷新页面不丢失登录状态）。`logout()` 清除所有状态和 localStorage。

---

### 37. `frontend/src/stores/flash.ts`（主版本）

- **路径**：`6.1版本/frontend/src/stores/flash.ts`
- **类型**：Pinia 状态存储
- **关联技术**：Pinia 2.3、TypeScript

管理全局消息提示。`show(msg, type)` 显示消息（支持 success/warning/error/info 四种类型），4 秒后自动消失。使用 `setTimeout` 和 `clearTimeout` 管理定时器，新消息会替换旧消息（取消旧定时器）。`App.vue` 中根据 `flash.type` 渲染不同颜色的提示条。

---

### 38. `frontend/src/stores/flash.js`（旧版副本）

- **路径**：`6.1版本/frontend/src/stores/flash.js`
- **类型**：Pinia 状态存储（⚠️ 建议删除）
- **说明**：这是 `flash.ts` 的旧版 JavaScript 副本。与 `.ts` 版本相比缺少 `let timer` 变量和 `clearTimeout` 逻辑——意味着旧版多个消息提示之间不会相互替换，时间线会叠加。建议删除此文件，仅保留 `flash.ts`。

---

### 39~62. `frontend/src/views/` — 页面组件（24个文件）

以下是所有 `.vue` 页面组件的逐一说明，按功能分组。

---

#### 公开页面（4个）

**39. `WelcomeView.vue`**
- **功能**：应用入口欢迎页。展示项目名称、简介和登录/注册入口按钮。用户未登录时的默认落地页（路由 `/` 重定向到 `/welcome`）。

**40. `LoginView.vue`**
- **功能**：用户登录页面。输入用户名+密码，调用 `POST /api/login`，成功后保存 Token 到 Pinia + localStorage，跳转到 Dashboard。页面加载时自动请求 `/api/captcha` 显示验证码图片（用于找回密码入口）。

**41. `RegisterView.vue`**
- **功能**：用户注册页面。填写用户名、密码、确认密码、邮箱、两个安全问题。前端校验格式后调用 `POST /api/register`。成功后提示跳转登录页。

**42. `ForgotPasswordView.vue`**
- **功能**：找回密码页面（两步流程）。第一步：输入用户名+验证码 → `POST /api/forgot_password` step1；第二步：回答两个安全问题+输入新密码 → step2 重置密码。

---

#### 仪表盘（1个）

**43. `DashboardView.vue`**
- **功能**：用户登录后的首页仪表盘。展示四大功能模块入口卡片：
  - **热带气旋观测分析**：直达 `/typhoon` 列表页
  - **地震活动监测分析**：直达 `/earthquake` 列表页
  - **龙卷风灾害记录分析**：直达 `/tornado` 列表页
  - **文献检索系统**：快捷检索输入框（跳转 `/search`）+ 热门关键词标签 + 用户头像引导个人中心
- **关联技术**：Tailwind CSS（毛玻璃卡片、渐变背景）、Font Awesome（图标）、Axios（调用统计和分类接口）

---

#### 文献检索系统（7个）

**44. `SearchView.vue`**
- **功能**：文献检索主页面。包含搜索栏（支持标题/作者/分类/关键词/全文五种检索类型切换）、分类筛选器（从 `literature_categories` 表加载）、日期范围筛选、全文过滤、分页导航。搜索结果卡片显示标题、作者、摘要、关键词和分类标签。点击进入 `DocumentView` 详情页。
- **关联技术**：Axios（调用 `/api/search`、`/api/categories`）

**45. `DocumentView.vue`**
- **功能**：文献详情页面。展示文献完整元数据、关键词标签、双向引用关系（被哪些文献引用 + 引用了哪些文献）、浏览/下载统计。操作区：收藏切换、PDF 下载、引用关系管理入口（仅上传者可见）。
- **关联技术**：Axios（`/api/document/{id}`、`/api/toggle_favorite`）

**46. `UploadView.vue`**
- **功能**：文献上传页面。选择分类标签（从 `CategorySelectView` 跳转或手动输入）、填写标题/作者/摘要/关键词/发表日期、选择 PDF 文件。提交时以 `multipart/form-data` 格式发送到 `POST /api/upload`。
- **关联技术**：Axios（FormData 上传，Content-Type 由拦截器自动处理）

**47. `CategorySelectView.vue`**
- **功能**：分类标签选择页面。展示全部 29 个预设分类（从 `/api/categories` 加载），用户点击选择后返回上传页。支持搜索过滤分类。
- **关联技术**：Axios（`/api/categories`、`/api/categories/search`）

**48. `MyDocumentsView.vue`**
- **功能**：我的文献列表。展示当前用户上传的全部文献，支持编辑（跳转 `EditDocumentView`）和删除操作。
- **关联技术**：Axios（`/api/my_documents`、`DELETE /api/document/{id}`）

**49. `EditDocumentView.vue`**
- **功能**：文献编辑页面。从 `/api/document/{id}/edit-data` 加载已有数据，编辑后 PUT 提交。仅上传者本人可访问。
- **关联技术**：Axios（GET 加载 + PUT 提交）

**50. `FavoritesView.vue`**
- **功能**：收藏列表页面。分页展示用户收藏的文献，点击进入详情页，支持取消收藏。
- **关联技术**：Axios（`/api/favorites`、`/api/toggle_favorite`）

---

#### 个人中心（2个）

**51. `ProfileView.vue`**
- **功能**：个人中心页面。三个 Tab：修改邮箱（验证当前密码）、修改密码（验证当前密码 + Werkzeug 哈希）、修改安全问题（验证当前密码）。与 `PUT /api/profile/*` 系列接口配合。
- **关联技术**：Axios、Werkzeug（后端）

**52. `StatsView.vue`**
- **功能**：数据统计页面。展示六维排行：关键词热搜 TOP10、活跃用户 TOP10、高被引文献 TOP10、热门浏览 TOP10、热门下载 TOP10、最多收藏 TOP10。从 `/api/stats` 获取数据。
- **关联技术**：Axios（`/api/stats`）

---

#### 台风模块（2个）

**53. `TyphoonView.vue`**
- **功能**：台风列表页。按年份、名称关键词、最低风速筛选。卡片网格展示每个台风的名称、季节、最大风速、最低气压、路径点数、起止时间。点击进入 `TyphoonDetail`。
- **关联技术**：Axios（`/api/typhoon/list`）、Tailwind CSS

**54. `TyphoonDetail.vue`**
- **功能**：台风详情页。三个核心区域：
  - **地图区**：Leaflet 地图展示台风路径（经纬度点连成折线，按风暴等级着色标注）
  - **强度时序图**：Canvas 2D 自绘双 Y 轴折线图——左轴风速（kts）+ 右轴气压（hPa），时间轴为 X 轴，带 `devicePixelRatio` 高清适配和 `ResizeObserver` 响应式重绘
  - **AI 分析区**：4 种分析类型按钮（趋势/对比/影响/出行建议），首次调用 DeepSeek，后续读缓存
  - **PostGIS 空间统计**：500km 范围内台风密度和强度统计
  - **年度统计入口**：跳转年度统计图表
- **关联技术**：Leaflet 1.9、Canvas 2D、DeepSeek API、PostGIS、Axios

---

#### 地震模块（2个）

**55. `EarthquakeView.vue`**
- **功能**：地震列表页。按年份、最低震级、地点关键词筛选。表格展示每个地震的震级、深度、地点、发震时间。点击进入 `EarthquakeDetail`。
- **关联技术**：Axios（`/api/earthquake/list`）

**56. `EarthquakeDetail.vue`**
- **功能**：地震详情页。四个核心区域：
  - **基本信息区**：震级、深度、坐标、海啸预警、误差参数等完整信息
  - **区域地震趋势图**：Canvas 2D 自绘散点折线图——以当前地震为中心，展示 ±3° 范围内邻近地震的震级随时间变化，鼠标悬停查看详情
  - **事件属性雷达图**：Canvas 2D 自绘多轴雷达图——将当前地震的震级、深度、误差等属性与区域历史均值对比
  - **AI 分析区**：3 种类型（趋势/风险/影响）
  - **PostGIS 空间统计**：300km 范围内地震密度、强震/大震次数
- **关联技术**：Leaflet 1.9、Canvas 2D、DeepSeek API、PostGIS、Axios

---

#### 龙卷风模块（2个）

**57. `TornadoView.vue`**
- **功能**：龙卷风列表页。按年份、地点关键词、最低 EF 等级筛选。表格展示 EF 等级、伤亡人数、地点、时间。附带可选年份和省份列表。
- **关联技术**：Axios（`/api/tornado/list`）

**58. `TornadoDetail.vue`**
- **功能**：龙卷风详情页。四个核心区域：
  - **基本信息区**：EF 等级、伤亡统计、经济损失（`_parse_damage` 格式化）、地理坐标
  - **EF 等级频次柱状图**：Canvas 2D 自绘柱状图——统计同省份龙卷风各 EF 等级的频次分布
  - **Leaflet 地图区**：标注当前龙卷风位置 + 同省份历史事件分布
  - **AI 分析区**：3 种类型（趋势/风险/气候）
  - **PostGIS 空间统计**：100km 范围内龙卷风数量、总伤亡、平均强度
- **关联技术**：Leaflet 1.9、Canvas 2D、DeepSeek API、PostGIS、Axios

---

#### 其他页面（4个）

**59. `HistoryView.vue`**
- **功能**：历史记录页面。双列表分页展示：搜索历史（关键词+时间）+ 浏览历史（文献标题+作者+查看时间）。从 `/api/history` 获取数据。
- **关联技术**：Axios

**60. `CitationView.vue`**
- **功能**：引用关系编辑页面（仅上传者可见）。搜索并选择被引用文献 → 保存引用关系到 `/api/save_citation/{did}`。展示已有的引用文献列表。
- **关联技术**：Axios（`/api/citation/{did}`、`/api/search_citation`、`POST /api/save_citation/{did}`）

**61. `ErrorView.vue`**
- **功能**：404 错误页面。捕获所有未匹配路由（`/:pathMatch(.*)*`），显示"页面未找到"提示和返回首页链接。
- **关联技术**：Vue Router

**62. `SearchView.vue`**（已在上文说明）

---

## 五、`.idea/` 目录

- **路径**：`6.1版本/.idea/`
- **类型**：IDE 配置目录（JetBrains PyCharm）
- **说明**：包含项目在 PyCharm IDE 中的模块配置、检查规则等。属于 IDE 自动生成的本地配置文件，不参与项目功能逻辑。`.gitignore` 中通常排除此目录。**不在技术栈说明范围内。**

---

## 附：文件与技术栈的对应关系

| 文件 | 所属层级 | 核心技术 |
|---|---|---|
| `backend/app/main.py` | 五、后端框架 | FastAPI, Uvicorn |
| `backend/app/core/config.py` | 辅助 | pydantic-settings |
| `backend/app/core/database.py` | 六、数据存储 | SQLAlchemy, psycopg2 |
| `backend/app/core/security.py` | 三、身份认证 | PyJWT |
| `backend/app/models/__init__.py` | 六、数据存储 | SQLAlchemy, PostgreSQL, PostGIS |
| `backend/app/routers/auth.py` | 三、身份认证 + 七、辅助 | PyJWT, Werkzeug, Pillow |
| `backend/app/routers/search.py` | 六、数据存储 | PostgreSQL tsvector+GIN |
| `backend/app/routers/document.py` | 七、辅助 | pdfplumber |
| `backend/app/routers/typhoon.py` | 六、数据存储 + 七、辅助 | PostGIS, DeepSeek API |
| `backend/app/routers/earthquake.py` | 六、数据存储 + 七、辅助 | PostGIS, DeepSeek API |
| `backend/app/routers/tornado.py` | 六、数据存储 + 七、辅助 | PostGIS, DeepSeek API |
| `backend/app/schemas/__init__.py` | 辅助 | Pydantic |
| `backend/app/services/__init__.py` | 七、辅助 | Pillow, requests, DeepSeek API |
| `frontend/src/main.js` | 一、前端框架 | Vue, Pinia, Vue Router |
| `frontend/src/App.vue` | 一+四、框架+视觉 | Vue, Tailwind CSS, Font Awesome |
| `frontend/src/api/index.js` | 三、通信 | Axios |
| `frontend/src/router/index.js` | 二、页面导航 | Vue Router |
| `frontend/src/stores/auth.js` | 二、状态管理 | Pinia |
| `frontend/src/views/TyphoonDetail.vue` | 四、视觉与交互 | Leaflet, Canvas 2D |
| `frontend/src/views/EarthquakeDetail.vue` | 四、视觉与交互 | Leaflet, Canvas 2D |
| `frontend/src/views/TornadoDetail.vue` | 四、视觉与交互 | Leaflet, Canvas 2D |
| `frontend/src/views/StatsView.vue` | 四、视觉与交互 | ECharts（待集成） |

---

> **文档版本**：v1.0 | **对应项目版本**：MeteoScholar v6.1
>
> © 2026 南京农业大学 · 信息与计算科学专业 · 丁俊杰
